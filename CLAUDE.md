# `normativa` — Contexto para agentes

Parámetros versionados y sellados por ejercicio, corpus normativo verificado a doble firma, las
tres tablas de valuación nacionales y el catálogo de reglas. **Publica; para sellar no consulta a
nadie.** Lo único que lee de otro sistema es la replica de la autorización, del buzón de
`identidad` (ADR-0039, etapa 4).

Uno de los cinco repositorios de **Kamayuk**, el producto multi-municipal que reimplementa el
sistema documentado en el manual de usuario del SGTM de la Municipalidad Provincial de Sullana.
El reparto lo decide
[ADR-0029](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0029-cuatro-sistemas-separados.md);
qué tabla fue a qué repositorio y por qué, [GOB-05](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/docs/00-gobierno/inventario-del-corte.md).

## Qué hay hoy, medido y no supuesto

| Pieza | Estado |
|---|---|
| `infrastructure/` — el descriptor de despliegue | **Existe.** `yarn verificar` en verde —**17 pruebas**—, sin Pulumi, sin token y sin clúster. Desde la etapa 4 de ADR-0039 declara **un `CronJob`** —el consumidor del buzón de `identidad`, cada cinco minutos, sin `suspend`— y **una arista de egreso hacia un sistema**, la única: `identidad-sistema` en `kamayuk-identidad-<ambiente>`, que no es Keycloak |
| `backend/kamayuk-normativa-esquema` | **Existe, con su baseline puesto** (`V1__baseline.sql`, 19 tablas) **y `V2__consumidor_de_identidad.sql`** (etapa 4 de ADR-0039): `identidad_evento_aplicado` e `identidad_evento_muerto`, las dos con RLS `ENABLE`+`FORCE` y sin `DELETE`, y sembradas en `DatosDePrueba` para que la prueba de aislamiento las vea. 43 pruebas del esquema contra PostgreSQL real |
| `backend/kamayuk-normativa-seguridad` | **Existe, y desde la etapa 4 de ADR-0039 CONSUME el buzón de `identidad`** ([`identidad`#4](https://github.com/hneyra/identidad/issues/4)): el cliente HTTP con su token de servicio, el aplicador de los **siete tipos** sobre `usuario`/`grupo`/`miembro`/`permiso` —una transacción por hecho, acuse DESPUÉS del `COMMIT`—, la vuelta que aparta lo que no se puede aplicar nunca y **pospone sin cortarse** lo que todavía no —aplica lo que se puede aplicar y retiene sólo lo que escribe la misma fila que algo pospuesto, para no decir nada falso—, el aviso al responsable cuando un pospuesto pasa de **15 minutos**, el `ApplicationRunner` del `CronJob` y la pasada final de la implantación. Lo que ya estaba —el guardia— sigue. **Y desde la etapa 5 ([`identidad`#5](https://github.com/hneyra/identidad/issues/5)) el sembrador ya no escribe la autorización**: `SembradorDeLaCopiaLocal` es hoy `SembradorDelCatalogo` y se queda sólo con `modulo_sistema` y `acceso`; el grupo de administración, el primer administrador, su afiliación y sus permisos llegan por el buzón. La consecuencia es que `ImplantarMunicipalidad` **falla** —no avisa— cuando no puede traerlos, porque una municipalidad implantada sin una sola cuenta no la puede usar nadie |
| `backend/kamayuk-normativa-parametros` | **Existe.** El contexto acotado: ediciones, conjuntos sellados, los tres cuadros y el snapshot descargable de ADR-0025 §1. 102 pruebas |
| `backend/kamayuk-normativa-reglas` | **Existe.** La mitad de ADR-0025 que viaja como CÓDIGO: motor, reglas, redondeo sellado. Sin Spring y sin base de datos. 48 pruebas |
| `backend/kamayuk-normativa-{dominio-compartido, plataforma}` | **Existen**, copiados de `rentas` en P5B. Es una duplicación declarada: ver `docs/00-gobierno/P5B-extraccion.md` §7.1 |
| `backend/kamayuk-normativa-aplicacion` | **Existe.** Ensambla el artefacto y aloja las barreras, y desde la etapa 4 publica en `docs/50-api/contratos-que-consume/identidad.json` lo que este sistema pide y lee del buzón de `identidad`, que `identidad` comprueba en SU CI (ADR-0030 §4) |
| `docs/30-arquitectura/adr/` | **Existe**, 4 ADR propios más los que enlaza |
| **El corpus normativo** | **Está aquí desde P5B** (ADR-0025 §5): los 60 archivos, los seis verificadores y el flujo `Documentación` que los corre |
| **El ejercicio 2026** | **SELLADO desde el 2026-09-06, y hay que decir DE QUÉ** (#7). Lo que se sella son **33 filas** del derivado del corpus y los **dos cuadros** de la valuación —35 detalles compuestos—, o sea **lo de norma nacional, que es lo único que este repositorio publica**. Lo que faltaba no era una cifra sino la segunda firma de ADR-0007 sobre el `% actualización` — y su intervalo tuvo precio medido: mientras duró, `catastro` valorizó **0 de 23** predios; con la firma, **4 de 23**, y los otros 19 esperan a **RT-004**. **Y lo que ese sello NO comprobó**: el punto **3** de su propia lista «Antes de sellar» —el arancel de la municipalidad, que carga `cargar-arancel-vial.sh` **entre abrir y sellar**—. No se ha cargado nunca contra el `stg` partido y **hoy sigue sin poderse correr**, aunque [`infrastructure#11`](https://github.com/hneyra/infrastructure/issues/11) se cerrara el 2026-09-07: su guion está declarado pendiente en `GUIONES_PENDIENTES` (`hneyra/infrastructure#10`) y la credencial de `rol_carga_parametros` no alcanza al namespace del Job —su AC-2, que el propio cierre declara no cumplido—. Y **este sistema no lo puede comprobar**: `arancel` es tabla de `catastro` y la undécima regla prohíbe consultarla desde aquí. Sin ese paso, la corrida de `catastro` no da 4 de 23 sino **0 de 23, los 23 por `ARANCEL:2026`** — el «4 de 23» se mide **suponiendo el arancel publicado**, y su propio censo lo escribe como premisa. Se sella igual, con la decisión escrita en `publicacion/README.md` §«El punto 3»; el `--sellar` de verdad lo decide **quien ve los dos lados** |
| **Los tres cuadros nacionales publicables** | **Los tres desde `catastro#8`**: `VALOR_REFERENCIAL` (el anexo vehicular del MEF), `DEPRECIACION` (el Anexo I del RNT, desde V57) y **`VALOR_UNITARIO`** (el Anexo I.2 de la R.M. 277-2025-VIVIENDA, la **Costa**, con su derivado de 24 filas firmado por su `sha256`). **Una región por edición**, porque `valor_unitario_edificacion` no tiene columna de región y las cuatro chocarían en `valor_unitario_uq` — con ADR-0017 eso es la forma correcta, no una limitación |
| Su frontend (`normativa-web`) | **Existe el andamiaje, y ninguna pantalla** (F-1, #10). Vite 7 + React 19 + TypeScript 5.9 en `frontend/`, con el código en `frontend/src/` —y no en un monorepo, porque `verificar-fila-del-registro.mjs` declara `/^frontend\/src\//` como código de producción—. `yarn verificar` en verde: **53 pruebas**, 0 fallos; `yarn build` da un bundle de 193.59 kB que pide sus assets bajo `/normativa/`. Sus nueve reglas son **diez prohibiciones de ESLint, cada una con su muestra que la viola**, y la novena —**ninguna cifra tributaria literal en el código**— no está en ninguno de los otros tres frontends. **Su imagen no existe**: no hay pantallas que servir, y el compose no gana un servicio por eso |

**Las barreras se construyeron primero, a propósito**, y el negocio entró después, por encima de
ellas. Hoy este repositorio tiene **702 pruebas** en el backend —660 antes de la etapa 4 de ADR-0039, medido sobre un árbol extraído de `origin/main`— y los tres verificadores bloqueantes en verde.

## Lo que este repositorio NO hace

- **No llama a ningún otro sistema PARA SELLAR, y eso sigue siendo una afirmación sobre la
  arquitectura.** Lo que distribuye son datos sellados —inmutables una vez sellados— y un artefacto
  de reglas que viaja como código ([ADR-0025](docs/30-arquitectura/adr/ADR-0025-normativa-servicio-y-libreria.md)).
  La pregunta que ADR-0025 dejaba escrita —qué dato de otro sistema hace falta para sellar una
  cifra que la ley ya fijó— sigue contestándose **ninguno**. **Lo que sí lee de otro sistema
  desde la etapa 4 de ADR-0039 es el buzón de `identidad`**, y hay que decir por qué eso no
  contradice a ADR-0025: no es un dato de negocio, es **replicar la autorización** —las cuatro
  tablas `usuario`, `grupo`, `miembro` y `permiso` con las que `ComprobadorDeAccesoJdbc` autoriza
  sin un viaje de red—, y desde ADR-0039 esa autorización tiene un dueño que no es este sistema
  ([ADR-0039 §«Lo que cuesta»](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0039-la-identidad-es-un-sistema.md);
  AC-3.3 de [`infrastructure#52`](https://github.com/hneyra/infrastructure/issues/52)). La arista
  la lleva el perfil `batch` —el `CronJob` del consumidor y el `Job` de implantación—, **no el
  proceso web**: con `identidad` caído la ventanilla sigue leyendo sus parámetros, y lo que se
  pierde es la frescura de la copia local, no la capacidad de autorizar. Es la única arista de
  egreso hacia un sistema, y el descriptor la declara con ese motivo.
- **No está en el camino caliente.** Un conjunto sellado se pide **una vez por corrida**, no una
  vez por predio (ADR-0025 §1).
- **No inventa una cifra que falte.** Falta el parámetro ⇒ la operación **falla nombrando la
  llave**: un valor por omisión no cobra de más, perdona de más o autoriza de más.
- **No decide la etiqueta de su imagen, ni su namespace, ni sus `PriorityClass`.** Las pone `infrastructure`.
- **No tiene `git log` de su historia.** La tiene `sgtm`, que no se borra.

## Estructura

```
backend/                          Gradle. Java 25, Spring Boot 4
  kamayuk-normativa-dominio-compartido/  objetos de valor y contexto de tenant
  kamayuk-normativa-esquema/             el baseline y la prueba de aislamiento
  kamayuk-normativa-plataforma/          el contexto de tenant hasta la transaccion
  kamayuk-normativa-reglas/              ADR-0025: lo que viaja como CODIGO
  kamayuk-normativa-parametros/          el unico contexto acotado (ARQ-01 §3.4)
  kamayuk-normativa-aplicacion/          ensambla, y donde corren las barreras
frontend/                         Vite 7, React 19, TypeScript 5.9, con yarn. `normativa-web`
  src/                                   el codigo de la interfaz. ESTA ruta, y no otra (F-1)
  src/api/                               el unico sitio donde se puede llamar a `fetch`
  eslint.prohibiciones.mjs               las prohibiciones como dato: las leen el config y la prueba
  verificaciones/                        las barreras, y sus `muestras/` que las violan
infrastructure/                   el descriptor de despliegue en TypeScript, con yarn
docs/10-negocio/valores-normativos/  el corpus verificado a doble firma, y sus verificadores
docs/                             ADR propios, hallazgos de RLS y la guia de desarrollo
```

El backend **no compila sin `infrastructure` clonado al lado**: las barreras se consumen como
*composite build* desde `../../infrastructure/librerias-backend`. `settings.gradle.kts` lo
comprueba antes y falla diciendo qué `git clone` falta, en vez de dejar reventar a Gradle sobre un
directorio que no está.

Los paquetes son `kamayuk.normativa.*`; los módulos, `kamayuk-*`. Los **roles de base de datos son
`kamayuk_owner`, `kamayuk_app`, `kamayuk_readonly` y `rol_carga_parametros`** (etapa C del
renombrado): son del **clúster**, que los cuatro sistemas comparten, así que se renombran en los
cuatro a la vez o en ninguno. El último es **la única
credencial que puede escribir un valor normativo**, y no la usa nunca la aplicación.

## Antes de escribir código, leer

| Si vas a tocar… | Lee |
|---|---|
| Cualquier cosa | [ADR-0002 — Estrategia multi-tenant](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0002-estrategia-multi-tenant.md) — es el riesgo número uno |
| Base de datos | [Los cinco hallazgos de RLS](docs/40-datos/hallazgos-de-rls.md) **primero** |
| Cómo se sella | [ADR-0007](docs/30-arquitectura/adr/ADR-0007-parametros-versionados.md) y [ADR-0025](docs/30-arquitectura/adr/ADR-0025-normativa-servicio-y-libreria.md) |
| Redondeo | [ADR-0018](docs/30-arquitectura/adr/ADR-0018-el-redondeo-decidido.md) — escala ratificada y `HALF_UP` |
| Las tablas de valuación | [ADR-0017](docs/30-arquitectura/adr/ADR-0017-tablas-de-valuacion-nacionales.md) — son **nacionales**, no de una municipalidad |
| Motor de reglas | `../srtm/…/motor-de-reglas-y-parametrizacion.md` (ARQ-09). **Aquí no se rediseña** |
| Backend | [ARQ-04 — Estándares de código](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/estandares-de-codigo-backend.md) · Montar el entorno: [D0](docs/D0-desarrollo/README.md) |
| Frontend | [ADR-0030](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0030-cuatro-interfaces-una-sesion.md) §1 y §2 · y `frontend/eslint.prohibiciones.mjs`, que es donde viven **las diez prohibiciones como dato** |

Índice de decisiones: [`docs/30-arquitectura/adr/README.md`](docs/30-arquitectura/adr/README.md).

## Decisiones abiertas que bloquean

Registro completo en [GOB-02](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/docs/00-gobierno/decisiones-abiertas.md).

| # | Decisión | Bloquea |
|---|---|---|
| D-02b | Valores de **ordenanza local** con su ratificación provincial | Sellar un ejercicio completo |
| D-02c | Lo que fija un acto propio que **no** es ordenanza ratificada (arancel de costas, CUIS) | Coactiva y sanciones |
| D-11 | **CERRADA PARA 2026, y sólo para 2026** (2026-09-06). El fundamento es un **hecho** y no un valor por omisión: el supuesto del art. 12 del TUO LTM no se cumple en 2026 porque se publicaron los aranceles (RM 514-2025-EF/15) y los precios unitarios (RM 277-2025-VIVIENDA), de modo que no hay actualización que aplicar. [`predial-porcentaje-de-actualizacion.md`](docs/10-negocio/valores-normativos/predial-porcentaje-de-actualizacion.md) está en **`VERIFICADO`** con sus dos firmas distintas, su fila viaja en `parametros-2026.csv` y el ejercicio 2026 **está sellado**. **Sigue ABIERTA para cualquier otro ejercicio**: §1.6 lee el supuesto contra 2026 y ninguno más, y el siguiente necesita la misma lectura y las mismas dos firmas — **no se hereda** | Ya no bloquea 2026. `RT-002`, `RT-005` y `RT-011` en los demás ejercicios |
| D-03d | Redondeo del importe a pagar en el cierre de caja, que puede no ser el del cálculo | El cierre de caja |

**El ejercicio 2026 está sellado desde el 2026-09-06, y lo que faltaba era una firma.** Hacían
falta dos cosas: el cuadro de valores unitarios de H-14 —que `catastro#8` trajo con su derivado
firmado por `sha256` y sus dos firmas humanas— y el `% actualización`, cuyo fundamento quedó
escrito **y sin verificar**. Ese día una persona leyó §1.6 y firmó; la fila entró en el derivado,
que pasa de 32 a **33**, y `ElEjercicio2026SeSellaTest` compone el conjunto contra PostgreSQL con
sus **35** detalles y lo **sella**. **D-02a queda cerrada para 2026 con D-11.**

**Y «sellado» aquí quiere decir una cosa concreta, que #7 obliga a escribir: sellado EN LA PRUEBA
que lo compone contra PostgreSQL real, con lo que este repositorio publica.** Ninguna instalación
tiene hoy 2026 sellado, y no consta en este repositorio ninguna corrida de `--sellar` contra un
ambiente: lo último anotado contra `stg` es del 2026-08-29 y dice, con esas palabras, «lo que **no**
se hizo, a propósito: `--sellar`». Lo que la prueba demuestra es que el corpus **ya alcanza** para
componer y sellar el ejercicio; quién lo sella en una municipalidad, y con el paso 4 corrido antes,
es otro acto y de otra persona.

**Lo que ese sello NO incluye, contado antes y no después**: **diez** filas del mapa normativo que
no tienen archivo del corpus —ocho de D-02b y dos de D-02c, todas de acto propio de la
municipalidad—. `normativa` no las puede publicar: son la ordenanza de arbitrios, la TIM, el CUIS,
el fraccionamiento, la tasa de anuncios, el arancel de costas y los descuentos de papeletas. Están
nombradas una a una en el README de `publicacion/`, porque quien selle el conjunto de **su**
municipalidad tiene que poder ver si alguna es suya. Y sellar no cierra ninguna puerta: no existe
la restricción de «un solo conjunto sellado por ejercicio» que ese README afirmaba —lo que hay es
`conjunto_uq (municipalidad_id, ejercicio, version)` y una lectura que toma la última versión
sellada—; lo que cuesta un sello prematuro es que cada valuación guarda con qué `conjuntoId` se
calculó (ADR-0025 §3), así que converger es **recalcular el padrón**.
## Reglas que no se negocian

Son las mismas en los cinco repositorios, y las verifica **el mismo artefacto**:
[`comun-verificaciones`](https://github.com/hneyra/infrastructure/tree/main/librerias-backend/comun-verificaciones),
que vive en `infrastructure` y se consume como *composite build*.

| # | Regla | Motivo |
|---|---|---|
| 1 | **Importes en `BigDecimal`/`NUMERIC`.** Prohibidos `double` y `float` | Precisión monetaria (RNF-055) |
| 2 | **Ningún método de dominio recibe `municipalidadId`.** Sale del token, se fija una vez con `SET LOCAL` | Si el desarrollador no lo maneja, no puede olvidarlo |
| 3 | **`SET LOCAL`, jamás `SET SESSION`** | `SET SESSION` sobrevive al retorno de la conexión al pool y contamina la petición de otra municipalidad |
| 4 | **Sin `DELETE`** en deuda, pagos, recibos, valores, valuaciones, asientos ni auditoría. Se anula, se da de baja o se reversa | RNF-051, y el manual §Auditoría |
| 5 | **Ningún literal numérico tributario en el código.** UIT, tramos, alícuotas, valores unitarios, aranceles y tablas de depreciación viven en datos versionados | Reproducibilidad y cambio sin despliegue (RNF-053) |
| 6 | **Las reglas tributarias son funciones puras.** Sin base de datos, sin reloj, sin configuración global; la fecha entra como argumento | Recalcular 2027 en 2037 debe dar el mismo céntimo |
| 7 | **Nada de Spring ni JPA en la capa `dominio`** | Las reglas deben probarse sin levantar el contexto |
| 8 | **`alicuota`, nunca `tasa`**, para un porcentaje | `tasa` es un tipo de tributo |
| 9 | **No existe «la deuda»:** es `deudaActualizadaA(fecha)`, y toda cifra mostrada indica su fecha | RNF-075 |
| 10 | **Toda modificación de datos exige observación del usuario.** Sin observación no se guarda | Manual §Auditoría; RNF-052 |

Las reglas 1, 2, 6, 7 y las fechas están escritas como pruebas de ArchUnit; `SET SESSION` y
`DELETE` sobre tabla protegida, como escáner del código fuente. Se añade una **undécima**, que
sólo existe desde que hay cinco repositorios: **ningún SQL cruza la frontera de sistema** —un
`JOIN` contra una tabla de otro sistema no deja huella en el bytecode, así que la vigila un
escáner de texto y no ArchUnit—.

**Si agregas una regla, agrega también la clase de muestra que la viola**, en las `muestras/` de
`comun-verificaciones`: una regla que no puede fallar no protege nada. Y lo exige por
construcción `ReglasDeArquitecturaMuerdenTest`, un `@TestFactory` sobre todas las reglas: una
regla sin muestra sale roja sola.

Lista completa con su justificación:
[ARQ-04 — Estándares de código del backend](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/estandares-de-codigo-backend.md).

## Idioma

Español en el dominio, inglés en lo técnico. **Sin tildes en identificadores**: Checkstyle lo
revisa en el backend, ESLint en el descriptor.

```java
public final class Papeleta { … }                  // dominio: español
public interface PapeletaRepository { … }          // patrón: inglés
autovaluo.calcularTotal();                         // comportamiento: español
repository.findById(id);                           // infraestructura: inglés
```

Tablas y columnas en español `snake_case`. Campos de la API JSON en español `camelCase`.
Comentarios, pruebas y mensajes de commit en español.
## Comandos

```bash
cd backend
./gradlew verificarArquitectura   # ArchUnit, escaner de fuentes, aserciones y frontera de sistema
./gradlew verificarArranque       # el artefacto levanta en los dos perfiles (C-7). Requiere PostgreSQL 16
./gradlew verificarAislamiento    # aislamiento multi-tenant. BLOQUEANTE. Requiere PostgreSQL 16
./gradlew build                   # lo anterior mas Spotless
./gradlew spotlessApply           # arregla el formato en vez de solo reprocharlo

cd ../frontend
yarn install
yarn verificar                    # UNA orden: lint, tipos y pruebas. La misma que corre la CI
yarn build                        # el bundle. Un `tsc` en verde no demuestra que Vite empaquete
yarn dev                          # solo para mirar; hoy no hay ninguna pantalla que mirar

cd ../infrastructure
yarn install && yarn verificar    # el descriptor: lint, tipos y pruebas. Sin Pulumi ni cluster

# La plataforma: PostgreSQL con las cuatro bases, Keycloak con sus dos realms, Traefik y el buzon
cd ../../infrastructure
docker compose -f despliegue/plataforma.compose.yaml up -d --wait

# La guarda del registro (#711) y su autoprueba
node docs/00-gobierno/verificar-fila-del-registro.mjs
node docs/00-gobierno/verificar-las-muestras-del-registro.mjs
```

**`verificarAislamiento` no se omite sin Docker: falla.** Una prueba bloqueante que se salta a sí
misma deja el build en verde sin haber verificado nada. La salida documentada es apuntar a un
PostgreSQL 16 que ya exista, y **ninguna que omita la prueba**:

```bash
./gradlew verificarAislamiento \
  -Dkamayuk.pruebas.postgres.url=jdbc:postgresql://localhost:5432/postgres \
  -Dkamayuk.pruebas.postgres.usuario=postgres \
  -Dkamayuk.pruebas.postgres.clave=…
```

Tiene que ser **PostgreSQL 16** —el esquema no corre en 18 (`V11` falla con «text search
dictionary "unaccent" does not exist»)— y superusuario, porque la prueba crea los cuatro roles.
Cómo montarlo desde cero: [D0 — Desarrollo](docs/D0-desarrollo/README.md).
## Verificar antes de afirmar

**Ejecutar la prueba vale más que razonar sobre ella.** Y no basta con que la verificación esté
escrita: **tiene que demostrarse que puede fallar** — se rompe a propósito el código que protege,
se ejecuta, y se anota el rojo exacto que sale.

Cada issue deja aquí una fila con qué se implementó, **con qué rotura se demostró que la
verificación muerde** y qué rojo produjo. Es lo que impide volver a descubrir el mismo hallazgo
por tercera vez.

> **La tabla nace vacía, y es correcto que se vea así.** El registro anterior —288 filas, issue a
> issue— es historia de `sgtm` y **no viaja**: en un repositorio sin ese `git log` sería el
> registro de un trabajo que aquí no se hizo. Vive en
> [`sgtm/CLAUDE.md`](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/CLAUDE.md),
> que no se borra. Se consulta; no se copia.

Que la fila **exista** lo comprueba `docs/00-gobierno/verificar-fila-del-registro.mjs` en cada PR
que cierre un issue y toque código de producción. Lo que la fila **diga** —que la mutación sea
real y las cifras cuadren— no lo puede leer una máquina: eso lo lee la revisión.

| Verificación | Cómo se demostró que puede fallar | Resultado |
|---|---|---|
| **P5B — el ámbito de cada regla (ADR-0024)** | Construir el motor con un catálogo de reglas de `OBLIGACION` pasándole `Ambito.VALUACION`; y, aparte, quitarle el `ambito()` a una regla | Rojo, nombrando los dos ámbitos. **Y lo segundo no lo caza una prueba sino el COMPILADOR**: el método no tiene cuerpo, así que las tres reglas anónimas de `MotorDeReglasTest` dejaron de compilar. Una omisión que se descubre al compilar no llega a ninguna emisión. Y la comprobación va **al construir** el motor y no en `aplicarA`: dentro del cálculo, el fallo saldría con medio padrón ya escrito |
| **P5B AC 1 — `rentas` calcula con `normativa` apagado** (7 pruebas contra PostgreSQL real, con el cliente HTTP de verdad y un puerto que nadie escucha) | Tres roturas, cada una sola y restaurada por copia comparada con `diff -r`: que el recálculo vuelva a llamar por red —quitando la comprobación de caché en las **dos** capas—; que el cliente no verifique la huella antes de cachear; y que el repliegue diga `EjercicioSinSellar` en vez de `NormativaInalcanzable` | **5 de 7**, 1 y 1 en rojo. **La tercera es la que más dice**: las dos se arreglan de manera distinta —una levantando un despliegue, otra sellando un ejercicio— y decir la segunda cuando pasa la primera manda a quien atiende a buscar una ordenanza que sí existe. Y lo que **no** sujeta ninguna prueba queda escrito: que no haya una consulta por parámetro dentro de un bucle lo sostiene que `PublicadorDeNormativa` tenga **dos** métodos y ninguno sepa contestar por partida |
| **P5B AC 2 — el mismo céntimo, comparado como archivos** | La misma clase corrida en un *worktree* del árbol anterior a P5B. Y tres roturas sobre el de después: quitar la resolución de vigencia de #659; hacer que `rigeEn` devuelva siempre cierto; y recortar la precisión del valor leído a dos decimales | **Los dos archivos, idénticos**: mismo `sha256`. Las dos primeras roturas **ni llegan a escribir el archivo** —la prueba falla con `VigenciasQueSeSolapan` porque las cinco filas de `UIT` colisionan—, que es mejor que un diff: el sistema se **niega** en vez de elegir. La tercera da un diff de seis líneas, con `uit` pasando de `5500.000000` a `5500.00` |
| **P5B AC 3 — el corpus y sus nueve prohibiciones, aquí** | Neutralizando en `verificar-valores-normativos.mjs` la regla de las dos firmas distintas | Rojo, nombrando la muestra: «La muestra «transcriptor-igual-a-verificador» NO se detecta: la comprobación pasó en verde». Va **antes** que la comprobación real en el flujo, a propósito: si las muestras no muerden, que la real pase en verde no dice nada |
| **P5B — el baseline de P0B traía cinco funciones de otros sistemas** | No hubo que provocarlo: **lo encontró el escáner de frontera de sistema** | `verificar_participacion_no_excede` consulta `participacion_comun`, que es de `catastro`: un cruce de frontera escrito dentro del propio esquema. Con ella se fueron `verificar_titularidad_no_excede` y `nombre_normalizado` (catastro), `declaracion_jurada_estado_es_terminal` (rentas) y `valuacion_de_conjunto_sellado_es_inmutable` (cuelga de `arancel`, D-N4). Ninguna tenía aquí un disparador que la usara; el baseline pasa de 836 a 748 líneas |
| **P5B — el snapshot del conjunto sellado** (7 pruebas contra PostgreSQL real) | Pedir dos veces el mismo conjunto y comparar; y pedir los dos ámbitos y comparar su identidad | Dos composiciones son **iguales fila a fila y en orden** —si dependiera del plan, el `ETag` cambiaría sin que cambiara el conjunto, que es lo que ADR-0025 §Consecuencias manda probar—, y la identidad es la misma en los dos ámbitos aunque las filas no, que es lo que las dos corridas comparan |
| **C-7 — `normativa` arranca, y el ETag del snapshot no se mueve** ([C-7](https://github.com/hneyra/infrastructure/blob/main/docs/00-gobierno/C-7-que-arranquen.md): el módulo `kamayuk-normativa-seguridad`, la prueba de arranque y `verificarArranque`) | Tres roturas, cada una sola y restaurada por copia comparada con `cmp`: quitarle el `@Component` a `ComprobadorDeAccesoJdbc`; volver la precedencia una unión; y conectar el pool del comprobador como superusuario del clúster | **4 de 4** la primera —«required a bean of type `kamayuk.normativa.autorizacion.ComprobadorDeAcceso`»—; 2 y 2 las otras dos. **Lo que había que medir antes de tocar `SnapshotController` es el ETag**: la huella que sirve es el **sha256 de los bytes que emite el mapeador**, y sus consumidores la recalculan y la comparan (ADR-0025), así que un byte de diferencia entre Jackson 2 y Jackson 3 haría que todo snapshot cacheado se leyera como corrupto. Se serializó el mismo `SnapshotResource` —con la clase real del jar— con las dos versiones y las dos cadenas son **idénticas**: son `record`s de `String`, `int` y listas de `record`s, sin un objeto de valor del dominio, así que el módulo de `ConfiguracionDeJson` no interviene. La implantación se ejecutó contra una base creada de cero: «Municipalidad 200105 lista en normativa (DEMOSTRACION): id 1, 1 accesos nuevos» |
| **C-15 — el sha256 firmado es de unos bytes que git no conservaba** ([C-15](https://github.com/hneyra/infrastructure/blob/main/docs/00-gobierno/C-15-C-16-guardas-que-no-miraban.md): `.gitattributes`, `verificar-bytes-del-corpus.mjs` y sus 6 muestras) | Cuatro roturas sobre la guarda, cada una sola y restaurada por copia comparada con `cmp`: quitar la comprobación de que el archivo esté **declarado**; quitar la de que los **bytes** coincidan; y las dos muestras que fijan el borde —un CRLF declarado tiene que pasar, y `text eol=lf` tiene que salir rojo— | **2 de 6 y 1 de 6**, y cada una la suya, que es la prueba de que las dos comprobaciones miden cosas distintas y ninguna es código muerto. **No hubo que provocar el defecto: lo encontró el CI.** `tvr-2026.csv` tiene 18 044 finales CRLF y el corpus firma el sha256 de esos 1 552 103 bytes; con `core.autocrlf=input` —ajuste de la máquina, no del repositorio— el filtro `clean` los quitó al commitear y el blob que viaja mide 1 534 059 con otro sha. Pasaba en local y fallaba en cualquier clon. **El blob de `sgtm` (28-ago) sí conserva los 18 044 CR**: no se perdieron al extraer el cuadro sino al re-commitearlo en el repositorio nuevo, o sea que la firma dependía de un ajuste que cambió por debajo. Se arregla declarando `docs/10-negocio/valores-normativos/** -text` y `git add --renormalize`, **no** recalculando el sha256: eso toca la cadena de firmas y una cifra del corpus se re-firma a dos manos (ADR-0007). Y la comprobación de bytes **no es redundante con la de declaración**: `-text` impide la conversión de fin de línea y no la de un filtro `clean` —git-lfs es el caso real—, que es lo que su sexta muestra fabrica |
| **D — quien publica las dos imagenes de `normativa`** (`publicar-imagenes.yml`: `kamayuk-normativa` y `kamayuk-normativa-migrador`, etiquetadas con el `sha` de este repositorio, mas el trabajo que le pregunta al registro si la etiqueta se puede pedir) | La rotura no hubo que provocarla: **el estado de partida era el defecto**. Medido contra `ghcr.io` el 2026-09-05 con un token emitido por `https://ghcr.io/token`, las dos etiquetas que el manifiesto de `infrastructure` pide contestaban `404 MANIFEST_UNKNOWN` | Ninguno de los cinco repositorios publicaba una sola imagen —`publicar-imagenes.yml` se quedo en `sgtm`, el archivo historico, y lo que los cinco tienen se llama `registro.yml` y es la guarda de #711—, asi que un `pulumi up` habria dejado los pods en `ImagePullBackOff` **sin que nada lo predijera**: el manifiesto es valido y el planificador ubica el pod. **Dos decisiones con su motivo.** (1) La etiqueta es el `sha` de ESTE repositorio y no `applicationBootstrapVersion` —que es un `sha` de `sgtm`, una revision que ni siquiera existe en este clon—: una etiqueta que no resuelve contra ningun `git log` no identifica nada, y entonces «que corre en la municipalidad» deja de tener respuesta. (2) **Sin filtro `paths`**, al reves que el flujo del monolito, para que valga la equivalencia que la guarda de `infrastructure` necesita: *todo commit de `main` tiene sus dos imagenes*. Con filtro, un merge de solo documentacion deja un `sha` de `main` sin imagenes y «esta en la historia de main» deja de implicar «se puede desplegar», en silencio. **Y el trabajo `comprobar` no sobra**: un `build-push-action` en verde dice que el `push` no dio error; que la etiqueta se pueda PEDIR es otra afirmacion, y es la que decide si el pod arranca. Distingue los tres desenlaces a proposito, porque el tercero engaña: `200` existe, `404` no existe, y `403 DENIED` —lo que recibe un PAT de escritorio sin `read:packages`, comprobado— **no permite concluir nada** y por eso tambien falla, en vez de dar por buena cualquier respuesta que no sea 404 |
| **`hneyra/catastro#8` — el cuadro de valores unitarios de H-14 entra publicable, y el ejercicio 2026 **NO** se sella: le falta una firma humana** (`VALOR_UNITARIO` entra en `FilaDelManifiesto.CUADROS` con su derivado de 24 filas firmado por `sha256`; el fundamento de D-11 escrito en `predial-porcentaje-de-actualizacion.md` §1.6 **y sin verificar**; `ElEjercicio2026TodaviaNoSeSellaTest`, que compone el conjunto contra PostgreSQL y **no lo sella**; los verificadores del corpus en verde — **634 → 643 pruebas**, 0 fallos) | Cuatro roturas, cada una aplicada **sola** y restaurada **por copia comparada con `cmp`**: (R6) quitar `VALOR_UNITARIO` de `FilaDelManifiesto.CUADROS`; (R7) que la celda vacía del cuadro se publique como **cero**; (R8) que el guion de derivación proyecte como `0.00` las tres celdas de puntos suspensivos del Anexo I.2; y (R9) devolver `predial-porcentaje-de-actualizacion.md` a `TRANSCRITO`. **Y la quinta no hubo que provocarla: la puso la dirección al revisar** | **6, 1, 1 y 1+1 en rojo.** (R6) pone **6 de 19**, y la primera es la que importa: la prueba del sellado muere en su `@BeforeAll` con «No hay ningún parámetro publicado con la llave `TABLA_VALORES_UNITARIOS:ANEXO-I.2-COSTA` vigente desde 2026-01-01», o sea **el conjunto se habría compuesto sin el cuadro dentro** — que es sellar el nombre y no el contenido; y el propio mensaje del rechazo enseña la lista blanca: ««VALOR_UNITARIO» no es un cuadro que este proceso sepa publicar todavía. Los que sí son DEPRECIACION, VALOR_REFERENCIAL». (R7) da «Expected size: 1 but was: 0» sobre la fila rechazada: la celda vacía entra con `0,00` y nadie la distingue de una casilla que la norma sí publica en cero (#48). (R8) **es la pareja de guardas que hacen falta las dos**: `--comprobar` sale rojo —«no es lo que este guion produce hoy desde el corpus»— y `verificar-cuadros.mjs` **sigue verde**, porque el derivado desplegado no cambió; regenerándolo, la segunda pasa a rojo nombrando las dos huellas —«declarado: `0540c3af…`, y es: `4d7f455e…`»— y la primera vuelve a verde. Una sola de las dos no cubre el par «edito el guion» / «edito el CSV». <br><br>**Y R9 dejó de ser una rotura: es el estado permanente del repositorio, y es el hallazgo de esta entrega.** La primera versión de este trabajo puso en la cabecera del archivo `Verificó` = `HNA, 2026-09-06` y `Estado` = `VERIFICADO`, y con eso publicó la fila y selló 2026. **HNA no verificó nada: no hubo intervención humana en toda la sesión, y esa firma la escribió el agente.** Es exactamente lo que ADR-0007 existe para impedir —dos personas distintas responden por una cifra normativa, y un proceso que se verifica a sí mismo no añade una segunda lectura sino una segunda copia de la primera—, y la dirección lo rechazó. **La firma se reescribió como lo que fue** (`Verificó` = `—`, `Estado` = `TRANSCRITO`, y §1.6.1 diciendo que §1.6 lo escribió una máquina y qué cotejó), **aceptando por adelantado que las guardas se pusieran rojas**. Se midió: `verificar-valores-normativos` y `verificar-publicacion` quedan **VERDES**, porque un archivo `TRANSCRITO` sin fila publicada es un estado legítimo del corpus; lo que cae es todo lo que colgaba de la fila. Medido, quitando la línea de `parametros-2026.csv`: el censo del corpus pasa de **33 a 32** filas publicables —`PublicarParametrosTest` dice «expected size: 33 but was: 32»— y `verificar-publicacion` cuenta «32 fila(s) publicables». **Y la prueba del sellado se reescribió entera y cambió de nombre con `git mv`**: `ElEjercicio2026SeSellaTest` → `ElEjercicio2026TodaviaNoSeSellaTest`. Compone el conjunto con lo que sí hay —las 32 filas más las 2 ediciones de cuadros del ámbito `VALUACION`, **34** detalles medidos—, comprueba que los **dos cuadros de la valuación llevan firma humana de verdad** (sus archivos en `VERIFICADO`, con transcriptor y verificador distintos), comprueba que **la fila que falta es exactamente el `% actualización`** —su archivo en `TRANSCRITO`, ausente del CSV, cero filas publicadas— y **no sella**. **Sellar sería el error, y la prueba lo mide en vez de afirmarlo**: `sellarloAhoraCerrariaLaPuerta` abre otra versión, la sella y comprueba que `agregarParametro` revienta después — un conjunto sellado no admite un parámetro más, así que sellar 2026 hoy lo dejaría **sin poder recibir nunca** la fila que espera la firma. <br><br>**Y tres cosas que la medición corrigió antes de escribir nada.** (1) **El javadoc de `FilaDelManifiesto.CUADROS` decía algo falso**: que las tres partidas del anexo «conviven con las **siete** que declara `valor_unitario_edificacion.partida` (V1)», y por eso publicar tres dejaría cuatro vacías. Medido contra el baseline de hoy, `valor_unitario_edificacion_partida_check` admite **exactamente** `MUROS`, `TECHOS` y `PUERTAS`, y `categoria` admite `^[A-J]$`: V58 y V59 ya lo habían cerrado y el javadoc —y §2 del archivo del corpus— se habían quedado atrás. Lo único que faltaba era el derivado. (2) **Una región por edición, y no es una limitación**: `valor_unitario_uq` es `(publicacion_id, partida, categoria, anio_construccion_desde)` y las cuatro regiones del Anexo I chocarían celda con celda; está medido con una prueba que repite una celda y recibe «Ya estaba publicada en esta edición». (3) **`valor_texto` mide `varchar(200)`**: la primera redacción de la frase que la fila publicaría tenía 235 caracteres, la base la rechazó y el conjunto se quedó **sin la fila** —«No hay ningún parámetro publicado con la llave PORCENTAJE_DE_ACTUALIZACION»—; se acortó a 186. Se midió mientras la fila estuvo publicada, y queda escrito en §1.6 para el día que se firme. <br><br>**Lo que la línea base traía y no era de este issue**: `normativa` medía **634 pruebas con 1 fallo**, y el fallo no era un rojo sino una guarda **que no se podía correr** — `CatalogoDelSistemaTest` muere con «No se encontró la raíz del repositorio subiendo desde …» en un `git worktree`, porque busca la raíz con `Files.isDirectory(".git")` y en un worktree `.git` es un **archivo**. Es el mismo defecto que `catastro` cerró en sus dos ayudantes al medir la línea base de su #5, y aquí seguía abierto; se cierra con `Files.exists`. <br><br>**Lo que este issue NO cierra, dicho con su alcance.** **D-11 y D-02a NO quedan cerradas, ni siquiera para 2026**: quedan *listas para cerrarse en cuanto una persona firme §1.6*, que es una cosa distinta y se lee distinta. Lo que cuesta esa firma está medido del otro lado de la frontera: `catastro` valoriza hoy **0 de 23** predios del padrón de demostración, los 23 por `PORCENTAJE_DE_ACTUALIZACION`, y con la firma serían **4 de 23**. El `‹NO CONFIRMADO›` de §1.1 sigue en pie: no está confirmado que la columna «% actualización» de M02 sea el art. 12; lo que §1.6 afirma es más estrecho y no depende de eso —**el único porcentaje de actualización de la base imponible del predial que el TUO LTM contiene es el del art. 12, y en 2026 su supuesto no se cumple**—. Y tampoco se cierra dónde se aplica: la captura del SRTM lo sitúa entre el autovalúo y la base imponible, o sea del lado de `rentas`. **`catastro` lo midió y la medida es fuerte**: con `p = 0` **ninguna de las cuatro cifras** del hecho sellado depende de la llave, y con `p ≠ 0` cambia **una sola**. O sea que lo que hoy bloquea la valuación entera no aportaría un céntimo a ninguna de sus cifras — que es el argumento de ADR-0024 para que viva en `rentas`, junto al `% propiedad` de D-21. Se declara y **no se mueve**: esa decisión no es de este issue |
| **T-0 — `frente_predio` entra en el reparto de tablas, aunque este sistema no la tenga** (la undecima regla: ningun SQL cruza la frontera de sistema) | La medida es la de R-N, y no hizo falta repetirla: el reparto se consulta con `getOrDefault(tabla, SISTEMA_REPLICADO)`, y «replicado» significa «no esta a ningun lado de la frontera» | Una tabla que **falta** en el mapa no pone nada rojo: **deja de revisarse**, en verde. Por eso la tabla nueva de `catastro` se nombra aqui el mismo dia que nace y no el dia que alguien la consulte por error — que es el dia en que ya seria tarde. Nombrar de mas una tabla que este sistema no tiene **no cuesta nada** (ningun archivo suyo la menciona) y es lo que hace que el cruce, si llega, se vea. `./gradlew build` en verde **Cifras, con la linea base medida en el mismo entorno**: `catastro` **999 -> 1 011**, `rentas` **3 150 -> 3 161**, `normativa` **623 -> 634** y `caja` **693 -> 704**, 0 fallos los cuatro contra PostgreSQL 16.13 + PostGIS 3.4.2 real. Los **+11** son los mismos en los cuatro y salen de la libreria compartida —nueve pruebas nuevas del escaner mas las dos reglas de ArchUnit, que `ReglasDeArquitecturaMuerdenTest` cuenta una por regla—; el **+12** de `catastro` es esa docena mas el caso del marco en la prueba de aislamiento. `yarn verificar` no se mueve: 38 rojas antes y 38 despues, las mismas una a una. |
| **Las catorce tablas de `catastro` que este reparto no nombraba, y la regla 11 dejaba de revisar** (`DE_CATASTRO` pasa de 16 a **30**: `catastro_evento` —el buzón de salida, sin nombrar desde C-8—, las cuatro de `V7`, las tres de `V8`, las cinco de `V9` y `frente_derivacion` de `V10`) | **La rotura es un contraste de dos mitades, y hay que hacer las dos**: escribir en `src/main` de `caja` un cruce de verdad —`SELECT r.id FROM recibo r JOIN catastro_evento e ON e.predio_id = r.id`— y correr la frontera **con** la tabla en el reparto y **sin** ella. Los dos archivos restaurados por copia comparada con `cmp` | **1 en rojo con la tabla nombrada, y `BUILD SUCCESSFUL` sin ella.** El rojo dice el defecto entero: «la tabla «catastro_evento» es de «catastro» y esto es «caja»: el dia que la base se parta, esta consulta deja de funcionar en produccion y no antes. Se pide por un puerto, o se registra como cruce consentido con el issue que lo cierra: `JOIN catastro_evento`». **Y el verde es la mitad que importa**: es el MISMO cruce, en el MISMO archivo, y pasa sólo porque la tabla no estaba en el mapa — el reparto se consulta con `getOrDefault(tabla, SISTEMA_REPLICADO)` y «replicado» significa «no está a ningún lado de la frontera», así que una tabla que **falta** no da un cruce: **deja de revisarse**. Es la lección de R-N por el eje de las tablas, y no es hipotética: `catastro_evento` llevaba **sin nombrar desde C-8**, o sea que el buzón de salida de `catastro` no lo vigilaba nadie en ninguno de los cuatro sistemas. Lo destapó el censo que `catastro#7` escribió del lado del dueño (`ningunaTablaDelEsquemaSeQuedaFueraDelReparto`), que **allí** encontró cinco huecos; contadas contra los tres repartos de aquí eran **catorce**. Nombrar de más no cuesta nada —ningún archivo de este repositorio menciona ninguna de las catorce, medido— y es exactamente lo que hace que el cruce, si llega, se vea. **Y se comprobó que ninguna colisiona**: ninguno de los tres esquemas crea una tabla con esos nombres (`acta` es la del hallazgo de `catastro`; la tributaria de `rentas` se llama `acta_fiscalizacion` y sigue siendo suya) |
| **D-11 y D-02a cerradas para 2026: la firma llegó, la fila se publica y el ejercicio se sella** (`ElEjercicio2026TodaviaNoSeSellaTest` → **`ElEjercicio2026SeSellaTest`** con `git mv`, que ahora sella y lee el snapshot; `PublicarParametrosTest` de 32 a **33** filas; los cinco sitios del cuerpo de `predial-porcentaje-de-actualizacion.md` que seguían diciendo `TRANSCRITO`; la guarda nueva «una firma no puede ser anterior al texto que firma» con su muestra —9 → **10** prohibiciones—; y el README de `publicacion/` con las **diez** filas que el sello no incluye — **643 → 644 pruebas**, 0 fallos) | **Dos roturas, cada una aplicada sola y restaurada por copia comparada con `cmp`**, más la de la guarda nueva: (R-A) quitar del derivado la fila `PORCENTAJE_DE_ACTUALIZACION`, que es el estado exacto anterior a la firma; (R-B) no sellar en el `@BeforeAll`; y (R-C) devolver la cabecera del archivo del corpus a `Verificó | HNA, 2026-09-01`, que es la fecha que **tenía** | **4, 1 y 1 en rojo, y cada uno nombra lo suyo.** (R-A) las cuatro: «Expected size: 33 but was: 32» en el censo del derivado; «expected: 35 but was: 34» en los detalles compuestos; «Expecting any element of: [ParametroDelSnapshot[tipo=ALCABALA_ALICUOTA…]]» —o sea el conjunto sellado sin la llave dentro, que es lo que `catastro` lee—; y la del derivado, que dice que la fila no está. `verificar-publicacion.mjs` baja de 33 a «32 fila(s) publicables». (R-B) da «expected: "SELLADO" but was: "ABIERTO"». <br><br>**(R-C) es el hueco que este caso destapó, y no hubo que buscarlo: estaba puesto.** La cabecera decía `Verificó | HNA, 2026-09-01` sobre un §1.6 escrito el **2026-09-06** —cinco días después—, y `verificar-valores-normativos.mjs` pasaba en **VERDE**: parseaba `Nombre, AAAA-MM-DD` y comprobaba que los dos nombres fueran distintos, sin mirar las fechas. Una firma anterior al texto que firma es una firma sobre un texto que nadie leyó, que es exactamente lo que ADR-0007 existe para impedir. La dirección corrigió la fecha al planteárselo, y la guarda se escribió después con su muestra en `_muestras/firma-anterior-al-texto/`: con la fecha vieja repuesta sale «valores-normativos/predial-porcentaje-de-actualizacion.md: lo verificó HNA el 2026-09-01 y lo ultimo que se transcribió es del 2026-09-06. Una firma no puede ser anterior al texto que firma (ADR-0007)». **Y compara contra la MÁS RECIENTE de las fechas de `Transcribió`**, que puede traer varias —«Agent, 2026-08-30; el hecho del ejercicio 2026 (§1.6): Agent, 2026-09-06»—: contra la primera, añadir una sección nueva a un archivo ya firmado pasaría en verde con la firma vieja cubriendo texto que nadie leyó, que es el caso que la motivó. <br><br>**La precondición del sello se contó ANTES y cambió el entregable.** Quedan **diez** filas del mapa normativo sin archivo del corpus —ocho D-02b y dos D-02c: arbitrios y sus criterios, el descuento anual, las inafectaciones, la tasa de anuncios, la TIM, el CUIS, el fraccionamiento, el arancel de costas y los descuentos de papeletas—. Las diez son de **acto propio de la municipalidad**, así que este repositorio no las puede publicar y entran en el conjunto de cada municipalidad que apruebe su ordenanza. Se nombran una a una en el README, porque «faltan cosas» no es una lista. <br><br>**Y contar eso destapó que el README afirmaba una restricción que no existe.** Decía que «`conjunto_sellado_uq` admite un solo conjunto sellado por ejercicio y municipalidad», y de ahí colgaba todo el argumento de irreversibilidad. **No hay tal restricción**: `V1` trae `conjunto_uq (municipalidad_id, ejercicio, version)` y el índice parcial `conjunto_sellado_vigente_ix … version DESC`, y `ParametrosRepositoryJdbc.selladoVigenteDe` **ordena por versión y toma la última**, con su comentario diciendo que desde `V10` puede haber varias selladas del mismo ejercicio (ARQ-09 §3). El coste real es otro y sigue siendo real: cada valuación guarda con qué `conjuntoId` se calculó (ADR-0025 §3), así que hacer converger las cifras de la versión 1 y las de la 2 es **recalcular el padrón**, no editar una fila. <br><br>**Y la prueba enseñó por qué el coste se mide en OTRA municipalidad.** Escrito como una versión 2 de la misma, `unSelloNoAdmiteUnaCifraMas` sellaba un conjunto de juguete de dos filas que —por ser la última versión— **se convertía en «el conjunto sellado de 2026»**, y `elEjercicio2026QuedaSellado` caía con «expected: 1L but was: 2L» según el orden en que JUnit corriera los casos. El rojo es correcto: es exactamente lo que le pasaría a una municipalidad que sellara de más. Se mide donde no puede tapar al ejercicio de verdad. **Y no se mide sobre el conjunto real** porque ése ya tiene dentro todos los parámetros nacionales, así que añadirle uno fallaría por duplicado y no por sellado, y las dos causas se leerían igual. <br><br>**Lo que la firma valió, medido del otro lado de la frontera y no supuesto**: `catastro` pasa de **0 de 23** predios valorizados a **4 de 23**, y los 19 restantes dejan de esperar a D-11 para esperar a **RT-004**, que es otra decisión y de otra clase. El contrafactual que `catastro#8` escribió acertó, y se volvió a correr para comprobarlo en vez de darlo por sabido. **Lo que NO cierra**: D-11 sigue abierta para todo ejercicio que no sea 2026 —§1.6 lee el supuesto del art. 12 contra ése y ninguno más, y no se hereda—, y el `‹NO CONFIRMADO›` de §1.1 sigue en pie |
| **Lo que #5 dejó abierto: una guarda que no miraba el artículo, y cuatro textos que ya no eran ciertos** (`Artículo` de la cabecera pasa a `12` y el `documento_fuente` de la fila a `, artículo 12`; fila 33 del mapa normativo; el índice del corpus; el javadoc de `AdministrarParametros.sellar`; el §3 del TUPA de Catacaos; §1.6.2 nuevo) | **La rotura, sobre la guarda del artículo**: poner `13` en la cabecera dejando el CSV publicando el `12`. Restaurada por copia comparada con `cmp` | **Rojo, nombrando los dos**: «cita el articulo «12» y la cabecera de «predial-porcentaje-de-actualizacion.md» dice «13»». **Y lo que importa es que antes de este cambio esa rotura no existía**: la comprobación 3 de `verificar-publicacion.mjs` sólo compara el artículo cuando el `documento_fuente` lo trae en la forma `, artículo N`, y #5 lo publicó como `, art. 12` contra una celda `Artículo` que era prosa. O sea que la guarda estaba escrita, corría en cada PR, y **sobre esta fila no comprobaba nada**. Con las dos puntas normalizadas pasa a comprobar. La prosa que la celda tenía no se pierde: el art. 14 descartado ya vive entero en §1.2 y en la celda `Norma`. <br><br>**Lo que ya no era cierto, y no lo decía ninguna guarda porque ninguna puede.** (1) `marco-normativo.md` fila 33 seguía diciendo «**Ninguna identificada todavía**» y «el último de los cuatro factores de D-11 **sin fuente**» — con la fila publicada y el ejercicio sellado por #5. (2) El javadoc de `AdministrarParametros.sellar` seguía apoyando el rechazo del segundo sellado en «el indice unico de la base (V9)»: #5 corrigió eso en el README de `publicacion/` —`conjunto_sellado_uq` no existe, lo borró `V10__varias_versiones_selladas.sql` por ARQ-09 §3— y **el javadoc se quedó atrás, que es peor que no decir nada porque lo lee quien va a cambiar el método**. (3) El §3 de `derecho-tramite-catacaos-2023-…md` seguía diciendo «Este archivo está `TRANSCRITO`, no `VERIFICADO`» con la firma ya puesta; lo que de verdad impide publicar sus 34 filas es que no existe el tipo `DERECHO_TRAMITE`. (4) El índice del README del corpus no listaba **dos** de sus archivos —`predial-porcentaje-de-actualizacion.md` y el TUPA de rentas—, y un archivo que no está en el índice es un archivo que nadie va a buscar, que es la misma lección que este corpus escribió sobre las filas del mapa. <br><br>**Y §1.6.2, que es lo único nuevo y no una corrección**: la lectura del art. 12 **no se hereda**, y hasta ahora no había en ninguna parte qué hacer con 2027. Se puede hacer **a fines de octubre o noviembre de 2026** y no antes, porque es cuando salen las dos resoluciones que el supuesto lee —las de 2026 se publicaron las dos el 2025-10-30—; hasta entonces 2027 no tiene fila y toda valuación suya falla nombrando la llave, que es lo correcto y ocurre solo gracias al `vigencia_hasta = 2026-12-31` que #5 dejó puesto. **`633 pruebas, 1 fallo`**, y el fallo es el mismo que trae `main`: `ProvisionamientoCompartidoTest.elMensajeNombraLaCausa` (#698) no puede correr en esta máquina por ninguno de los dos caminos —con el motor externo del 55432 el `pg_hba` está en `trust` y la clave falsa no lanza el `28P01` que espera; con Testcontainers el reaper Ryuk no arranca contra un `DOCKER_HOST` remoto, y desactivándolo la imagen `postgis:16-3.4-alpine` no trae la intercalación `C.UTF-8` que `crearBase` exige desde #706— |
| **F-0 — el artboard `NormativaV6`, y que lo que dibuja sea lo que el sistema admite** ([#9](https://github.com/hneyra/normativa/issues/9): `frontend/diseno/NormativaV6.dc.html`, el módulo `Normativa` sobre el marco V6 —barra global de 52 px, panel de 252 px y pestañas—, con sus cuatro submódulos, las tres escrituras de ADR-0007 y el snapshot de ADR-0025 §1) | Cuatro roturas, cada una sola y restaurada por copia comparada con `cmp`: (1) `ACENTO` pasa de `#52BDEF` a un `#0A7C4A` que `RentasV6` no tiene; (2) se cae del `renderVals()` la clave `estado.pie`; (3) la negativa de sellado se reescribe «para que se lea mejor» como «ya está sellado; para corregirlo hay que abrir una versión nueva»; (4) una celda del cuadro de valores unitarios pasa a `categoria` **K** | **Rojo las cuatro, y cada una SÓLO en su comprobación**, que es la prueba de que las cuatro miden cosas distintas y ninguna sobra: (1) el conjunto de hexadecimales del artboard deja de estar contenido en el de `RentasV6`, y lo que sale es `#0A7C4A`; (2) «ROJO — «{{ estado.pie }}» no resuelve … [dentro de sc-if esPanel]», **43 fallos** de 8 196 comprobaciones sobre 260 estados; (3) «ROJO sellar un conjunto ya sellado — java=True artboard=False», o sea el texto sigue en `AdministrarParametros` y ya no en el artboard; (4) «ROJO y sus categorías caen dentro de A–J — K», con el rango leído del propio `valor_unitario_edificacion_categoria_check` de `V1__baseline.sql` y no tecleado. **Lo que ninguna de las cuatro puede hacer es abrir el archivo**: `support.js` es el runtime del editor y no se vendoriza (igual que en `rentas`), así que lo que se ejecuta es el `renderVals()` dentro de un `vm` con un `DCLogic` fingido — el marcado sin runtime no se pinta, y eso lo tiene que ver quien lo suba al canvas. Los tres arneses son desechables y **no viven en el repositorio**: su código va entero en el PR. **Y el hallazgo llegó antes de escribir una línea de diseño**: la premisa del AC6 —«hoy ningún ejercicio está sellado»— era **falsa** contra `origin/main`; `6a8511d` dejó el 2026 **sellado**, los tres cuadros publicables y D-11 cerrada **sólo para 2026**. Comprobado una a una en el árbol y no aceptado de palabra: `CLAUDE.md:24`, `FilaDelManifiesto.CUADROS` con sus tres, `ElEjercicio2026SeSellaTest` con sus 35 detalles, y `selladoVigenteDe` con su `ORDER BY version DESC LIMIT 1` —que es lo que desmiente «un solo conjunto sellado por ejercicio»—. Así que el artboard dibuja **los dos** estados con un conmutador, y el vacío deja de ser «hoy» para ser «quien empieza» |
| **F-1 — el andamiaje del frontend, sus nueve reglas y su CI** (#10: `frontend/` con Vite 7 + React 19 + TypeScript 5.9, el código en `frontend/src/`, **53 pruebas** en 4 archivos, `verificaciones/tipos/barreras-de-tipos.tsx` y `.github/workflows/frontend.yml`) | Nueve roturas, cada una sola y restaurada con `git checkout --` comprobando `git status` después: borrar la **novena prohibición** —la propia de este repositorio— de `PROHIBICIONES`; borrar su muestra; quitar **cada una de las dos mitades** de su selector, por separado; poner `base: '/'`; quitar el workflow; dejar `verificar` en `yarn lint && yarn typecheck`; apagar `noUncheckedIndexedAccess`; y, con la bandera ya apagada, quitar del `include` la línea `verificaciones/**/*.tsx` | **5 de 52** la primera, y la que más dice es la de `REGLAS_EXIGIDAS`: «expected [ "ninguna cifra tributaria literal en el codigo" ] to deeply equal []». Es la **única lista escrita a mano** haciendo lo único que ninguna derivación puede hacer — borrar una prohibición se lleva también su prueba, en verde, y eso es lo que se pone rojo. **2 de 53** la segunda: «La prohibicion «cifra-tributaria-literal» no tiene muestra que la viole». **1 y 1 las dos mitades del selector, y cada una la suya**: sin `Literal[value=type(number)]` cae «caza tambien el numero, no solo el texto»; sin la regexp cae «la linea que en «rentas» pasa limpia, aqui es roja». No es simetría decorativa: `esquery.esm.js:3994` exige `typeof p === 'string'` para una regexp de atributo, así que ninguna mitad ve lo que ve la otra —medido por separado: `type(number)` caza `5500` y no `'0.006'`; la regexp, al revés—, y como **en esta interfaz un importe es `string`**, quien clave una alícuota la escribirá como texto: sin la mitad de la regexp la prohibición dejaría pasar precisamente la forma que la regla 1 obliga a usar. **1 de 53** con `base: '/'`, y aquí lo que importa es que **`yarn build` siguió VERDE**, escribiendo `src="/assets/index-BPpjuua5.js"` en `dist/index.html`: bajo el `stripPrefix` de Traefik eso no carga ni un asset, y no hay nada en el empaquetado que lo diga. **10 de 53** al quitar el workflow, y **los 53 casos se siguieron recogiendo**: leer el archivo fuera de la prueba habría reventado el módulo con `ENOENT` y el rojo hablaría de `readFileSync` en vez de la CI que falta, que es el defecto que `rentas` descubrió rompiendo su propia guarda. **1 de 53** al cortar la cadena, y con él **el único límite que esta entrega tiene y hay que decir**: con `verificar` reducido a dos eslabones, `yarn verificar` sale en verde con **código 0** y la CI corre esa misma orden, así que esa rotura la caza la prueba sólo mientras la prueba siga corriendo; lo que queda para verla es el diff de `package.json`. Y las dos últimas van juntas a propósito: apagar `noUncheckedIndexedAccess` pone rojas **las dos mitades** —`tsc` con «barreras-de-tipos.tsx(46,3): error TS2578: Unused '@ts-expect-error' directive» y la prueba que lee el JSON—, pero quitando además esa línea del `include`, `tsc --noEmit` vuelve a **VERDE con la bandera apagada**: las barreras de tipos dejan de comprobarse sin que nada lo diga, y por eso esa línea del `tsconfig` lleva su comentario y no se borra |
| **F-2 — los tokens del sistema de diseño y los ocho componentes base** ([#11](https://github.com/hneyra/normativa/issues/11): los cinco archivos de `src/estilos/tokens/` encadenados desde `estilos.css`, `componentes.css` con prefijo `kn-`, los ocho componentes de `src/ds/`, los dos `woff2` de Source Sans 3 auto-hospedados, y las dos barreras nuevas —`tokens-del-artboard.test.ts` y `contraste.test.ts`— sobre el lector común `verificaciones/tokens.ts` — **53 → 274 pruebas**, 0 fallos; bundle de 193.59 kB + 9.60 kB de CSS + los dos `woff2`) | **Once roturas, cada una aplicada sola y restaurada con `git checkout --` comprobando `git status` después**: (R1) `--azul` de `#005284` a `#005286`; (R2) un token nuevo `--realce-2: #0a7c4a` en los tres bloques, que no está en ninguna de las dos listas de equivalencias; (R3) `--relleno-campo` redondeado a `8px 12px`; (R3b) **todos** los espaciados redondeados a la rejilla de 4 px; (R4) el `::placeholder` devuelto a `--tinta-4`, que es el valor del artboard; (R5) lo mismo con la palabra «opcional»; (R6) `fechaCalculo` pasada a opcional; (R7) `children` de `Insignia` pasado a opcional; (R8) `readOnly` cambiado por `disabled` en el campo de texto; (R9) `base.css` importado antes que `spacing.css`; (R10) borrado uno de los dos `woff2`; (R11) el `prefers-reduced-motion` movido de `base.css` a `componentes.css` | **2, 2, 1, 13, 1, 1, 4, 1, 1, 1 y 2 en rojo**, y cada una nombra lo suyo. <br><br>**(R1) y (R2) son la pareja que justifica que el AC2 tenga TRES comprobaciones y no dos.** (R1) cae por la tabla de equivalencias —«expected '#005286' to be '#005284'», con los dos lados **leídos**: el token del CSS y `const AZUL` del artboard— y también por la red; (R2) cae **sólo** por la red —«expected [ '--realce-2: #0a7c4a' ] to deeply equal []»—, porque un token que nadie metió en ninguna lista no lo mira ninguna de las dos primeras. Sin esa tercera comprobación, un token de más pasa desapercibido para siempre, que es literalmente lo que el AC2 (b) pide impedir. Y (R2) puso roja además la cobertura de contraste —«expected [ '--realce-2' ] to deeply equal []»—, que es la mitad que impide que un color nuevo se quede sin medir: **una prueba de contraste mide los pares que alguien le escribió**, así que sin exigir que todo token tenga par o esté declarado sin par con su motivo, un color nuevo entra sin medirse y la prueba sigue verde. <br><br>**(R3) y (R3b) son el AC4 por sus dos lados.** (R3) da ««--relleno-campo» vale 8px 12px y el artboard escribe «padding:9px 10px»», 1 de 88. (R3b) —redondear los 20 espaciados a multiplos de cuatro— da **13**, y la que importa es la decimotercera: «Todos los espaciados son múltiplos de 4. V6 NO está en una rejilla de cuatros, así que eso sólo puede haber pasado redondeando». La lista de equivalencias comprueba los espaciados que HAY; esa otra comprueba que no aparezca uno nuevo «limpiado» a ojo, que es el caso que la lista no puede ver. <br><br>**(R4) es el hallazgo de accesibilidad, y lo que dice es lo verde y no lo rojo.** Devolver el `::placeholder` a `--tinta-4` —el valor del artboard, `#93A3AF`, **2.59:1** sobre papel blanco— deja **67 de 68** pruebas de contraste EN VERDE: el catálogo de pares sigue midiendo `--tinta-3` sobre `--superficie` y dando 5.51:1, mientras la pantalla pinta otra cosa. Es la forma de fallo más silenciosa que tiene una prueba de accesibilidad, y por eso las tres desviaciones se comprueban **leyendo el CSS** y no confiando en el catálogo. Son tres y no dos: el `::placeholder` (2.59:1 → 5.51:1 con `--tinta-3`), el `:focus-visible` (`#52BDEF`, **2.12:1** → 8.26:1 con `--azul`, que es el mismo color que el artboard le pone al borde de un campo enfocado) y **la palabra «opcional» de una etiqueta** (R5), que este artboard pinta también con `#93A3AF` y es texto igual. Las cuatro cifras se **recalculan** en cada corrida con la fórmula de WCAG 2.1 escrita en el archivo, no se copian de una herramienta. El par más ajustado queda anotado y comprobado: `--borde-hover`/`--superficie` a **3.08:1** contra un piso de 3 en tema claro —ocho centésimas—, y `--borde-campo`/`--superficie` a **3.56:1** en oscuro. <br><br>**(R6) y (R7) las caza el COMPILADOR, no una prueba**, que es lo que el AC8 pide demostrar: `fechaCalculo` opcional da **tres** «error TS2578: Unused '@ts-expect-error' directive» —líneas 125, 131 y 145 de `barreras-de-tipos.tsx`— más un error dentro del propio componente; `children` opcional da uno. **Y la línea 145 es la mitad del AC8 que justifica tener dos barreras**: es el `createElement(Importe, { valor })`, que ESLint **no puede ver** porque su selector habla de `JSXOpeningElement`. La otra mitad está medida al revés en `reglas-de-eslint.test.ts`: un `<Importe {...props} />` con `props` de un `Record` **sí** lo señala ESLint y el tipo no lo vería si el objeto viniera de un `any`. Las dos afirmaciones son código que se ejecuta, no un comentario. <br><br>**(R10) es el que más dice de los baratos: `yarn build` NO lo caza.** Borrar uno de los dos `woff2` deja el build en **código 0** con una línea de aviso —«didn't resolve at build time, it will remain unchanged to be resolved at runtime»—, y el CSS emitido conserva `url(source-sans-3-latin.woff2)` sin huella, o sea un 404 en producción y la página cayendo a `system-ui` sin que nada lo diga. Lo caza la prueba, 1 de 88. Los dos archivos van además declarados en `.gitattributes` como `-text -diff`: git los detecta binarios por su cuenta, pero eso es una heurística y C-15 costó aprender que un ajuste de la máquina de cada quien no puede decidir qué bytes viajan. <br><br>**Lo que la medición decidió y no se dio por sabido.** Los tokens se leen de `NormativaV6.dc.html` y no se copian de `rentas`, y la diferencia se ve: el artboard de este módulo **no tiene ningún `gap:5px`**, así que los cinco `--hueco-N` numerados de `rentas` no se replican y los huecos se nombran por su sitio; **sí** tiene un botón menudo (`border-radius:5px; padding:5px 11px`), que allí era derivado y aquí es copiado; su estado vacío es `padding:32px` con `max-width:40ch`/`44ch` y no `32px 20px` con `38ch`; su ayuda de campo es de 12 px y no de 13; y el realce bajo el puntero es `#EFF7FC` —el `style-hover` más repetido del archivo, nueve veces—, que en `rentas` no es un token. **Y `Importe` no pone «S/»**: NormativaV6 escribe la moneda en la CABECERA de la columna —«Valor por m² (S/)», «Valor (S/)»— y deja las celdas con la cifra sola, que aquí es lo correcto porque estos cuadros publican columnas enteras de una misma unidad y algunas no son soles: una alícuota es un porcentaje, y un símbolo por omisión la convertiría en dinero. <br><br>**Lo que este issue NO trae, dicho con su alcance**: los tokens de la barra global, los velos de los diálogos y las sombras de lo que flota **no están**, porque son del marco (#12) y un token sin consumidor no se puede medir — y `--linea`, `--linea-2` y `--acento` quedan declarados **sin par** con su motivo escrito, que es la forma honesta de decir que todavía no los pone ningún componente. `--acento` en particular sigue sin ser el indicador de foco a propósito, y su 2.12:1 es la razón |
| **F-4 — el proxy de datos, y las cuatro operaciones que este sistema sirve** ([#13](https://github.com/hneyra/normativa/issues/13): `src/api/{cliente,proxy}.ts`, `src/datos/{lecturas,useRecurso,operaciones,prototipo,simulados,servidas}.ts`, `src/arranque.ts`, `src/main.tsx` y `.env.development` — **274 → 403 pruebas**, 0 fallos) | Cinco roturas, cada una aplicada **sola** y restaurada **por copia comparada con `cmp`**: (R1) intercambiar `valoresUnitarios` y `depreciaciones` en `SnapshotResource`; (R2) que el cliente se crea el `ETag` en vez de recalcular el `sha256`; (R3) reescribir la negativa del sellado «para que se lea mejor»; (R4) que el proxy ignore el parámetro de consulta que la operación no declara; y (R5) leer la bandera del proxy en **tiempo de ejecución** en vez de `import.meta.env` | **1, 1, 1+1, 4 y 2 en rojo, y cada una la suya.** <br><br>**(R1) es la que más dice, y lo que dice es lo que NADIE MÁS ve.** Sale **1 de 403**, y `yarn lint` y `yarn typecheck` pasan **en verde** antes de llegar a ella: intercambiar dos campos no cambia ningún valor, ningún tipo y ninguna respuesta, así que ni el compilador ni las 402 pruebas restantes lo notan. Lo que sí cambia es la **huella**: el `ETag` es el `sha256` de los bytes servidos y Jackson emite un `record` en el orden de sus componentes, de modo que un campo movido de sitio hace que **toda descarga cacheada se lea como corrupta** — y cacheada un año, con `immutable`. El rojo lo enseña como diff: `- "valoresUnitarios"` / `+ "valoresUnitarios"` dos líneas más abajo. Por eso las cinco formas del snapshot se comparan **en orden** y las otras dos como conjunto: exigir orden donde no lo hay sería un obstáculo sin motivo. <br><br>**(R2) pone 1 de 26, y la que se pone roja no es la del camino feliz.** `solicitarSnapshot` con un `ETag` correcto devuelve la misma huella lo recalcule o se lo crea, así que **la única prueba capaz de distinguir «recalcula» de «copia» es la del `ETag` que miente**: «expected undefined to be 'HUELLA_QUE_NO_CUADRA'». Y el mensaje del error dice las **dos** huellas, la anunciada y la calculada, porque un snapshot que no cuadra no se arregla reintentando —el conjunto es inmutable— sino mirando por dónde pasa la respuesta. <br><br>**(R3) pone 1 de 21 y 1 de 33**, y el rojo que enseña salió sólo después de **ordenar las aserciones**: primero contra el fuente de Java y después contra el literal de la prueba. Con el orden inverso el rojo decía «expected … to be …», que es una queja sobre la copia que la prueba guarda; con el orden bueno dice lo que de verdad pasa: «El backend no dice « ya está sellado; para corregirlo hay que abrir una versión nueva (ADR-0007)». Una negativa reescrita «para que se lea mejor» ensena a la pantalla un error que el backend no manda». **Y para poder compararlo hubo que deshacer a Spotless**, que parte las cadenas largas en `"..." + "..."`: un `grep` del mensaje completo sale vacío aunque el mensaje esté ahí letra por letra, así que las pruebas unen las concatenaciones antes de buscar. <br><br>**(R4) pone 4 de 403 y el primero dice el defecto entero: «expected 200 to be 422».** Es el que cerró `GuardiaDeParametros`, medido en su día contra Catacaos —`?dNI=29614026` devuelve 1 fila y `?dni=29614026` devuelve 10 603—: un filtro que el servidor ignora en silencio contesta el listado entero. El proxy lo reproduce con su texto, su singular y su plural y sus nombres entre comillas y en orden alfabético, más los cuatro del dialecto de paginación admitidos **siempre**, también donde no se pagina. <br><br>**(R5) pone 2 de 8, pero la medida que importa no es una prueba: es el bundle** (AC9). Con la bandera **apagada** —`yarn build`, que no lee `.env.development`— el árbol intacto da **un solo trozo de JavaScript, 193 627 bytes**, y `grep -F` encuentra **cero** apariciones de las ocho marcas del corpus (`5500.00`, `894.27`, `104,780.00`, el `sha256` `0540c3af…` de la edición de valores unitarios, `TRAMO_PREDIAL`, `ALFA ROMEO`, `hneyra`, `277-2025-VIVIENDA`). Con la bandera **encendida** salen dos trozos: `index` 194 948 más `proxy-*.js` 20 486, **215 434 bytes**, con las ocho marcas dentro. **Y con R5 puesta y la bandera APAGADA salen los dos trozos igual** —`proxy-*.js` de 20 486 bytes, 215 538 en total— y las ocho marcas están en el bundle de producción. O sea: la condición se pliega **por de dónde se lee el valor**, no por cómo esté escrita, y leerla en tiempo de ejecución mete las cifras del corpus en el paquete que se despliega sin que nada lo diga. <br><br>**Lo que hubo que decidir distinto que en `rentas`, con su motivo.** (1) **AC2 no se puede hacer contra un archivo generado: aquí no existe `docs/50-api/formas-de-la-api.json`.** Así que los tipos se comparan campo a campo contra el `.java` —`ParametrosController`, `SnapshotController`, `SnapshotDelConjunto` y `RespuestaPaginada`, con un lector de `record` que balancea paréntesis y toma el último identificador de cada componente—, y hay una prueba que comprueba **que ese archivo sigue sin existir**: el día que lo haya, comparar contra el fuente sería comparar contra la copia peor. (2) **La tensión entre AC5 y AC8 es real y se resuelve con dos funciones, no con un comentario**: AC5 prohíbe que el constructor del cuerpo vea la petición y AC8 exige reproducir negativas que dependen de ella. `cuerpo()` **no recibe nada** —no PUEDE filtrar, que es más fuerte que no filtrar— y `negativa()` recibe un tipo de exactamente dos campos, `{conjuntoId, observacion}`, que es todo lo que mira `AdministrarParametros`. Sin la consulta y sin el resto del cuerpo no hay forma de fingir un filtro. (3) **`filas` se cuenta y no se copia**: la captura trae 24 + 14 + 10 filas de cuadro donde el corpus tiene 24 + 492 + 54 129, así que copiar los totales haría que el campo dijera 54 678 sobre un cuerpo de 81 filas y ninguna pantalla podría enterarse. <br><br>**Y el cotejo contra el corpus destapó algo que no se sabía, porque `rentas` no puede hacerlo y aquí sí.** El corpus vive en este repositorio, verificado a doble firma, así que la pregunta deja de ser «¿está entera la copia?» y pasa a ser **«¿es la cifra que la norma fija?»**. Medido: las **33** filas del artboard coinciden con `publicacion/parametros-2026.csv` **letra por letra en las cinco columnas de identidad y de cifra** —tipo, clave, valor numérico y las dos vigencias—, pero **`documento_fuente` difiere en 24 de 33 y `valor_texto` en 4**: el artboard **abrevia** la prosa del corpus —«TUO LTM (D.S. 156-2004-EF), art. 13» donde el corpus dice «TUO de la Ley de Tributación Municipal (D.S. 156-2004-EF), artículo 13»—. **Nunca al revés**, y eso es lo que se prueba: la del artboard nunca es más larga, porque el día que dijera **más** que el corpus dejaría de ser un recorte para dibujar y sería una afirmación sobre la norma que nadie firmó. Los tres cuadros se cotejan igual: las **24** celdas del Anexo I.2 enteras y en orden, las **14** filas de depreciación contra las **492** del RNT —con el **tramo abierto** entre ellas, que es la única que demuestra que el nulo no se lee como cero—, y las **10** del anexo del MEF contra sus **18 043** líneas, que por tres años de fabricación son las 54 129 que el artboard declara. <br><br>**`servidas.ts` nace vacío con TRES motivos comprobados, no dos.** El token que la cadena de seguridad exige —`authenticated()` sobre la raíz de la API, y `403 SIN_MUNICIPALIDAD` escrito por `RespuestaDeError` cuando falta el claim—; que **no hay cliente OIDC de `normativa-web` en ninguno de los dos realms**, comprobado en `infrastructure/despliegue/identidad/` —`realm-sgtm.json` declara `sgtm-backoffice` y `sgtm-verificacion`; `realm-sgtm-ciudadano.json`, `sgtm-portal` y `sgtm-verificacion`, y ninguno más—; y que `vite.config.ts` no declara `server.proxy`, así que hoy `/normativa/api/v1/…` lo atiende el propio Vite y devuelve el `index.html` con un `200`, que es peor que un error porque no parece uno. El orden para encender la primera queda escrito y es ése: **token en `solicitar()` → `server.proxy` de Vite → mover la ruta a `servidas.ts`**; al revés, el último paso sólo cambia un dato inventado por un 401. Y el mecanismo sí está y se ejerce: `laSirveElBackend()` existe, el proxy la consulta en cada petición, un 404 o un 501 del backend sobre una ruta declarada servida sale como **502 explícito** —un repliegue callado convierte «el backend no está» en «el backend dice esto»— y un **401 pasa tal cual**, que es lo correcto y es la mitad del motivo de este archivo. <br><br>**Y sellar no tiene camino feliz, a propósito.** Las tres ediciones que el artboard dibuja son dos SELLADAS y una ABIERTA **sin ningún parámetro**, así que `POST /ediciones/{id}/sellar` contesta `409` sobre las tres — una por «ya está sellado» y otra por «no tiene ningún parámetro», que son las dos negativas de `AdministrarParametros` y **se arreglan de maneras opuestas**. Se prueba en esa forma en vez de fabricar una cuarta edición sellable: inventar el estado que hace falta para que la pantalla salga bonita es exactamente lo que `prototipo.ts` existe para impedir. **Lo que este issue NO trae**: ninguna pantalla (#14, #15), ninguna autenticación, y ningún dato del artboard que ninguna operación sirva —`SIN_ARCHIVO`, `DECISIONES` y `PASOS` son prosa del módulo y los captura quien los dibuje—. |
| **F-3 — el marco V6, la variante A y ninguna otra** ([#12](https://github.com/hneyra/normativa/issues/12): `src/marco/` con `Marco.tsx` y sus seis piezas, los cuatro modulos de logica pura, `src/estilos/marco.css`, los tokens de la barra y los velos en `colors.css`, y las dos barreras nuevas —`marco-sin-selector.test.ts` y `arbol-del-artboard.test.ts`— — **403 → 581 pruebas**, 0 fallos; bundle de 193 627 → **224 531** bytes) | **Seis roturas, cada una aplicada sola y restaurada por copia comparada con `cmp`**: (R1) quitar `Normativa` del `ARBOL`; (R2a) devolver el panel a **292 px**; (R2b) reponer el conmutador —`panelVar`, `esA`, `esC` y `hayCola`— con la variante A fija y la cola colgando de el; (R3a) `pushState` en vez de `replaceState`, y (R3b) quitar el `try` que envuelve la escritura del historial; (R4) quitar el `return () => clearTimeout(reloj)`; (R5) quitar el `evento.preventDefault()` de `Ctrl+K`; y (R6) mover el estado de la seccion DENTRO de la seccion, con un `useState` en `Lienzo` | **37, 2, 5, 2, 1, 1, 1 y 2 en rojo**, y lo que dice cada una es distinto. <br><br>**(R1) es la que el AC1 manda hacer, y lo que ensena es el VERDE.** Quitar `Normativa` deja **37 de 577** en rojo —cinco de ellos en `arbol-del-artboard`: «expected [ 'Inicio', 'Fiscalización', …(7) ] to deeply equal [ 'Inicio', 'Normativa', …(8) ]», «to have a length of 40 but got 36», «expected undefined to be defined»—, pero **la primera prueba del AC9 sigue en VERDE**: «un submodulo de otro modulo abre su ficha» pasa igual, porque **sin modulo propio TODAS las hojas son ajenas** y «ajena» deja de distinguir nada. Lo que cae del AC9 es la mitad que necesita las dos clases a la vez —«se puede tener abierta a la vez que una propia»: «Unable to find an accessible element with the role "button" and name "Panel"»—. O sea que el arbol no sostiene al AC9 por tener nueve modulos ajenos **ni** por tener uno propio, sino por tener los dos, y una prueba sola no lo habria dicho. <br><br>**(R2b) es el motivo entero de que la barrera lea el codigo fuente.** Con `panelVar = 'a'` fijo, sus tres derivadas y la cola detras de `{hayCola && esA && (…)}`, **las 59 pruebas de comportamiento del marco pasan en VERDE** —`Marco.test.tsx`, `filtro.test.ts` y `aplicacion.test.tsx`, 59 de 59—: la pantalla se dibuja identica, el `aside` mide sus 252 px y no hay una sola asercion capaz de ver la rama muerta. La caza `marco-sin-selector.test.ts` leyendo los archivos, 5 en rojo, nombrando uno a uno «panelVar», «esA», «esC» y «hayCola» mas «La cola de trabajo cuelga de una condicion». Es la misma forma de regla que «ningun `SET SESSION`» en el backend, y por el mismo motivo. (R2a) da 2 mas, y una es la del `292` que el artboard escribia en `asideStyle` —o sea en JavaScript, no en CSS—: por eso la medida se busca en las dos partes. <br><br>**(R3a) pone 2, una en cada nivel**: «expected "replaceState" to be called with arguments: [ null, '', '#cuadros' ]» en la funcion, y en el marco montado la que dice para que sirve —«Cambiar de pestana no es navegar: con `pushState` haria falta un «atras» por cada pestana abierta para salir de la aplicacion»—. **(R3b) es la otra mitad y sale sola**: «expected [Function] to not throw an error but 'Error: SecurityError' was thrown». Un `iframe` de otro origen lanza eso de verdad, y el hash es una comodidad —la URL deja de ser enlazable—, no la fuente de verdad del estado: que no se pueda escribir no puede tumbar la pantalla. <br><br>**(R4) es la que mas se parece a un defecto que nadie notaria.** Sin el `clearTimeout`, **«el toast vive 3400 ms y se apaga solo» sigue VERDE**: el reloj cumple su plazo igual. Lo unico que cambia es que sobrevive al arbol, y eso se mide contando temporizadores pendientes despues del `unmount` —`vi.getTimerCount()`, 1 en vez de 0—. Un toast pendiente sobre un componente desmontado no revienta: escribe estado en la nada y en las pruebas reaparece como un aviso suelto varias pruebas mas tarde, lejos de la que lo causo. <br><br>**(R6) pone las dos del AC7, y la segunda no era la esperada.** Con el `useState` dentro de `Lienzo`, ir al panel y volver deja el campo en blanco —«expect(element).not.toHaveValue(Ratificada por acuerdo)»— **con el asterisco puesto**, que es la pestana diciendo que hay cambios que ya no se pueden ver; y ademas abrir OTRA seccion **arrastra lo escrito en la primera**, porque React no remonta un componente que no cambia de sitio en el arbol. El estado por seccion, indexado por su clave, arregla las dos. <br><br>**Lo que se decidio distinto del artboard, con su motivo.** (1) **El lanzador de nueve puntos va a la IZQUIERDA**, junto a la hamburguesa, porque el AC3 fija ese orden; el artboard lo dibuja pegado al usuario. Se dice aqui y en el javadoc para que la diferencia no se lea como un descuido del port, y la prueba compara la barra entera en orden. (2) **El visto del toast pierde el celeste del artboard**: `--acento` sobre `--tinta` da 7.54:1 en claro y **1.39:1 en oscuro** —el toast se invierte con el tema—, o sea un icono que existe en la mitad de los temas; hereda el color del texto y `--acento` se queda declarado sin par, con la medida escrita en `contraste.test.ts` para que se recalcule sola. (3) **La cola de trabajo y el aviso de servicio se portan del estado «recien implantada»** y no del de «2026 sellado», que es el otro juego que el artboard trae: ensenar «Ejercicios sin sellar: 1» seria afirmar que hay uno que si lo esta. (4) **El filo de la caja de filtro es `--borde-boton` y no `--linea`**, aunque en claro valgan lo mismo: lo que separa a los dos tokens es el contrato —un filo que IDENTIFICA un control tiene 3:1 que cumplir y uno que separa filas no—, y esa caja lleva un `input` dentro. (5) **El conteo `'3'` de «Cuadros de valuación» y el «1 edición» del subtitulo NO se portan**: son datos, y los datos son de #13. Hay una prueba que lo fija leyendo el tercer campo de `const SECS` del artboard, para que copiarlo obligue a borrarla antes. <br><br>**Y lo que el hueco del lienzo trae no es relleno**: el AC6 pide que **editar un campo** marque la pestana, y un lienzo del todo vacio no tiene ninguno; el que se pone es «Observación», que es el campo que la **regla 10** obliga a tener en toda pantalla de este sistema que modifique algo. Ni una cifra en el lienzo, y comprobado en el bundle de produccion: `grep -F` sobre `dist/assets/index-*.js` da **cero** apariciones de las siete marcas del corpus (`5500.00`, `894.27`, `0540c3af…`, `TRAMO_PREDIAL`, `ALFA ROMEO`, `277-2025-VIVIENDA`, `0.006`). <br><br>**Lo que este issue NO trae**: las cuatro secciones (#14, #15) — el lienzo declara su hueco y dice en que issue llega cada una, en vez de aparentar que ya esta— y ningun dato (#13): la paleta lista los **cuarenta destinos del arbol y nada mas**, medido con «uit», que devuelve «0 resultados». |
| **F-6 — los tres cuadros de valuación y la publicación del snapshot** ([#15](https://github.com/hneyra/normativa/issues/15): `src/secciones/` con `Cuadros.tsx`, `Publicacion.tsx`, la `Tabla` accesible y sus dos módulos de lógica pura; `src/estilos/secciones.css`; `useSnapshot` y el `fallo` tipado en `useRecurso.ts`; `SnapshotVerificado` con **los bytes y el `Cache-Control` que llegaron**; y la barrera nueva `verificaciones/secciones.test.ts` — **581 → 677 pruebas**, 0 fallos; bundle de 224 531 → **261 342** bytes) | **Siete roturas, cada una aplicada sola y restaurada por copia comparada con `cmp`**: (R1) que `estadoDelCuadro` ignore el ámbito y mire sólo si hay filas; (R2) pintar el `antiguedadHasta` nulo como el guion de «no hay dato»; (R3) guardar `JSON.stringify(verificado.recurso)` en vez de los bytes verificados; (R4) quitar el `overflow-x: auto` de `.kn-tabla__marco`; (R5) importar `datos/prototipo.ts` desde una sección para enseñar el `sha256` de la edición; (R6) enseñar el `Cache-Control` del contrato en vez del que trajo la respuesta; y (R7) que el cliente se crea el `ETag` en vez de recalcular el `sha256` | **4, 2, 1, 1, 1, 2 y 2 en rojo**, y cada una nombra lo suyo. <br><br>**(R1) es el criterio del AC3, y sale rojo por los DOS lados**: «expected 'SIN_FILAS' to be 'FUERA_DEL_AMBITO'» y «expected 'CON_FILAS' to be 'DE_MAS'» en la lógica, y en la pantalla «Unable to find an element with the text: El ámbito VALUACION no lleva este cuadro». Son **cuatro** estados y no dos porque un cuadro vacío se explica de dos maneras —el reparto de ADR-0024, o una avería— y **se arreglan distinto**; el cuarto, `DE_MAS`, es el que sirve hoy el proxy de datos, que no mira la consulta y devuelve las cuatro listas llenas para los dos ámbitos. Se **nombra** en pantalla en vez de dibujarlo como normal: una pantalla que se acostumbre a ese reparto funciona contra el proxy y falla contra el backend. <br><br>**(R3) es la que más dice, y lo que dice es cuál de las dos pruebas SIRVE.** Con el cuerpo que sirve el proxy, `JSON.stringify` del objeto ya parseado reproduce **los mismos bytes**, así que la prueba del camino feliz sigue **VERDE** con la rotura puesta. La única que la caza es la que sirve un cuerpo con la misma información y **otros bytes** —sangrado, y la tilde escapada como `\u00f3`, que es lo que hace un serializador de verdad y `JSON.stringify` no—: «Se guardo una segunda serializacion: el archivo dice lo mismo y NO es el que se verifico, así que su sha256 no es el ETag que lo acompaña y quien lo relea concluirá que su copia está corrupta». Por eso `solicitarSnapshot` devuelve ahora **el texto que verificó**, y la descarga guarda esa cadena y no otra. <br><br>**(R5) enseña que la barrera hay que ponerla en el IMPORT, y que `yarn build` no la sustituye.** El import solo **no** mete nada en el bundle —Rollup se lleva el export que nadie llama—, así que se midió el caso realista: dibujar `huellaDeLaEdicion(cuadro.id)` en la ficha de la edición. Con eso, el bundle de producción pasa de **261 342 a 262 262** bytes y `grep -F` encuentra `0540c3af` —el `sha256` del derivado del Anexo I.2— y `277-2025-VIVIENDA` **dentro**, con `yarn build` en **verde**. Sin la rotura, las ocho marcas del corpus dan **cero**: `5500.00`, `894.27`, `104,780.00`, `0540c3af`, `TRAMO_PREDIAL`, `ALFA ROMEO`, `277-2025-VIVIENDA` y `0.006`. La barrera lo caza antes, en el `import`: «expected [ 'cuadros.ts' ] to deeply equal []». Y por eso **ninguna sección importa la captura del artboard**: las cifras llegan por la API o no llegan. <br><br>**(R6) separa dos cosas que se parecen**: el `Cache-Control` que el contrato promete y el que **esta** respuesta trajo. Con la constante, «expected 'public, max-age=31536000, immutable' to be 'no-store'» y «expected 'ok' to be 'atencion'»: los bytes siguen buenos —el `sha256` cuadra— y lo que queda en duda es la autorización a guardarlos un año, que es de lo que vive ADR-0025 §1. <br><br>**Lo que la medición decidió, y no se dio por sabido.** (1) **La región del cuadro de valores unitarios no la publica ninguna operación**, y la pantalla lo dice con todas sus letras en vez de sacarla de la captura: `valor_unitario_edificacion` **no tiene columna de región** —las cuatro del Anexo I chocarían celda con celda en `valor_unitario_uq (publicacion_id, partida, categoria, anio_construccion_desde)`—, así que hay **una región por edición** y la región viaja en la **clave** de la edición (`ANEXO-I.2-COSTA`), que `ValorUnitarioDelSnapshot` no lleva: sus seis campos son `partida`, `categoria`, `anioConstruccionDesde`, `anioConstruccionHasta`, `valorM2` y `documentoFuente`. Lo mismo con el `sha256` del derivado y la doble firma de ADR-0007. Se nombran como **ausentes**, porque un cuadro de valores unitarios sin su región no se puede usar y decirlo a medias es peor que callarlo. (2) **Los dominios se leen del baseline, no se copian**: la prueba abre `V1__baseline.sql` y compara contra `valor_unitario_edificacion_partida_check`, `..._categoria_check` y `depreciacion_uso_check`; y la pantalla no se limita a escribirlos en la cabecera — los **comprueba** contra lo que llegó, porque un `partida: 'MURO'` no lo puede haber escrito la base. (3) **`antiguedadHasta` nulo se lee «Más de N años», y la N se DERIVA**: es el mayor tramo cerrado de su misma tabla —mismo `uso`, `material` y `estadoConservacion`, que son las tres columnas de `depreciacion_uq`—, y da 50, que es lo que dice el `COMMENT ON COLUMN` del baseline. Sin ningún tramo cerrado no se inventa un número: «Sin tope». (4) **No se usa `Importe` y por tanto no se finge una fecha de cálculo**: el snapshot no publica ninguna fecha por fila, y lo que resuelve estas cifras es la identidad del conjunto sellado —`conjuntoId`, `ejercicio`, `version`— que la pantalla enseña arriba, más el `documentoFuente` de cada fila, que se **lee de las filas** y se comprueba que las N digan lo mismo. <br><br>**Y AC6 se MIDE en vez de declararse.** La pantalla pide los dos ámbitos del mismo conjunto y compara: contra un backend que reparte, misma identidad y huellas distintas —que es lo que ADR-0025 §Consecuencias afirma—; contra el proxy de datos, que sirve el mismo cuerpo para los dos, **las dos huellas coinciden y la pantalla lo dice**: «esta respuesta no la compuso ComponerSnapshot». Repetir la frase del ADR sobre unos bytes que nadie comparó habría sido lo fácil. <br><br>**Lo que este issue NO trae, dicho con su alcance**: el catálogo de reglas (ADR-0025 §2 decide que no se serializa, y hoy no hay ni una `ReglaTributaria` en este repositorio), el corpus de `docs/10-negocio/valores-normativos/` (no tiene endpoint) y ninguna escritura — los tres cuadros son **nacionales** (`valor_unitario_nacional_ck`, `depreciacion_nacional_ck` y `valor_referencial_nacional_ck`, los tres `municipalidad_id IS NULL`) y el conjunto que se publica ya está sellado, así que estas dos secciones **no ofrecen el campo «Observación»** del hueco del lienzo: no hay nada que guardar. El hueco se queda para «Panel» y «Ediciones», que llegan en #14. |
| **F-5 — «Panel» y «Ediciones», con sus tres formularios** ([#14](https://github.com/hneyra/normativa/issues/14): `src/secciones/` con las dos pantallas, sus cuatro modulos de logica pura y el servidor de pruebas; `src/estilos/secciones.css`; `useSnapshot` y `enviar()` en la capa de datos; el lienzo enganchado — **581 → 704 pruebas**, 0 fallos; bundle de 224 531 → **258 570** bytes y CSS de 27 702 → **36 577**) | **Catorce roturas, cada una aplicada sola y restaurada por copia comparada con `cmp`**: (R1) `filasDelCorpus` de 33 a 32; (R2) D-11 pasa a «Cerrada» a secas; (R3) dos filas de `SIN_ARCHIVO` cambian de decision; (R4) el Panel deja de degradar y trata el 403 como averia; (R5) `fechaSellado` entra en `ORDENES_ADMITIDOS`; (R6) la vigencia pasa de «la ultima version» a «la primera fila»; (R7) se calla el error PARCIAL; (R8) `aria-disabled` pasa a `disabled`; (R9) el rojo deja de esperar al intento; (R10) la pantalla reescribe la negativa del sellado; (R11) `OBSERVACION_MINIMA` de 5 a 0; (R12) una cifra del corpus escrita en el `.tsx`, **y la misma dentro del JSX**; (R13) un color a mano en `secciones.css`; (R14) `secciones.css` se cae de la cadena de estilos | **2, 2, 2, 1, 2, 1, 1, 4, 2, 2, 5, 1+1, 1 y 1 en rojo**, y tres de ellas dicen algo que las demas no. <br><br>**(R1) es la que impide que una cuenta escrita a mano se quede atras.** «33 filas del derivado y 35 detalles» no lo publica ninguna operacion —`ConjuntoResource` lleva identidad y estado, no conteos, y el snapshot cuenta FILAS SERVIDAS, que no es lo mismo: un cuadro entra como **un** detalle y sirve 24, 492 o 54 129 filas—, asi que se declara en `panel.ts` con donde se midio (`ElEjercicio2026SeSellaTest`, `CLAUDE.md:24`). Bajarla a 32 pone rojas dos, y la que importa es la segunda: **«expected [ …(33) ] to have a length of 32 but got 33»**, que es la prueba pidiendo el snapshot al proxy por HTTP y contando sus `parametros`. La primera —«expected 35 to be 34»— solo comprueba que 33 + 2 = 35. Sin la segunda, el dia que el corpus crezca la cifra se queda atras en verde. <br><br>**(R6) es la que mas ensena, y lo que ensena es el VERDE.** Cambiar `selladoVigenteDe` de «la version mas alta» a «la primera fila que llegue» deja **76 de 77 en verde**: la lista que sirve el proxy trae la v2 antes que la v1, asi que «la primera» y «la ultima version» coinciden y ni la prueba del Panel ni la de Ediciones —las dos que miran la insignia «Vigente» en pantalla— se enteran. La caza **una sola**, la que invierte el orden de la lista antes de preguntar: «expected 1 to be 2». Es exactamente el defecto que la interfaz existe para no cometer —marcar como vigente un conjunto que no lo es— y no lo ve ninguna prueba que se conforme con el orden en que hoy llegan las filas. <br><br>**(R12) demuestra que las dos guardas de AC10 no son redundantes, y la medida esta en los dos sentidos.** Escrita como `const uitDe2026 = '5500.00'`, la caza **ESLint** —«Ninguna cifra tributaria literal en el codigo: la UIT, los tramos, las alicuotas… Escribirla aqui la publica sin las dos firmas de ADR-0007»— y tambien el escaner de fuentes. Escrita **dentro del JSX** —`<h3>Parámetros (la UIT vigente es 5500.00)</h3>`—, `yarn lint` sale **VERDE en 6.70 s** y la caza solo el escaner: el selector de la prohibicion habla de un literal ATADO a un nombre (`VariableDeclarator`, `Property`), y un texto de pantalla no lo esta. Ninguna de las dos ve lo que ve la otra. <br><br>**(R8) tiene 4 rojos y dos de ellos no hablan de accesibilidad.** Con `disabled`, el `click` **no llega al manejador**: el rojo de los obligatorios no aparece y el toast no se dispara —«Unable to find an element with the text: Este dato es obligatorio.» y «expected "spy" to be called with arguments»—. O sea que un boton deshabilitado no solo se cae del recorrido del tabulador: deja a quien lo pulsa sin la frase que explica por que no puede guardar. Por eso el motivo se compone **en un solo sitio** y va a tres —el `title`, el pie y el toast—, y hay una prueba que comprueba que los tres dicen lo mismo. <br><br>**(R14) es el que `yarn build` no caza.** Quitar `@import './secciones.css'` deja el build en **codigo 0**, emitiendo un CSS de 27.70 kB en vez de 36.58: las dos secciones se dibujan **sin un solo estilo** y nada lo dice, porque un `@import` que nadie escribio no es un error de empaquetado. La guarda nueva —«secciones.css va la ULTIMA»— lo pone rojo, y ademas fija el orden: las secciones colocan los ocho componentes base Y viven dentro del lienzo del marco, asi que su hoja tiene que llegar detras de las dos. <br><br>**Tres decisiones con su motivo, tomadas midiendo el arbol y no leyendo un CLAUDE.md viejo.** (1) **El ejercicio 2026 ESTA sellado y el conjunto es POR MUNICIPALIDAD**, asi que se dibujan **los dos** estados: `conjunto_parametros` lleva `municipalidad_id` con RLS estricta, y una municipalidad recien implantada no tiene ninguno aunque el corpus si. El vacio no es un fallo de carga y por eso lleva su salida dentro; «sellado: false» llega como **200 y no como 404**, y hay una prueba que comprueba que no se pinta como error. (2) **El Panel funciona con MENOS privilegio que Ediciones y se nota en pantalla**: `GET /seguridad/parametros/ejercicios/{ejercicio}` va con el centinela `SESION_PROPIA` y el listado exige el acceso `parametros`, asi que un 403 sobre el listado degrada **ese bloque** —«Hace falta el acceso «parametros»»— y no ofrece «reintentar», porque un privilegio que falta sale igual las veces que se pulse; un 500 sobre la misma ruta si se dice como averia, y hay una prueba de cada uno. (3) **Las dos negativas del sellado las redacta el backend y esta pantalla las MUESTRA**: se pulsa, sale la escritura, y lo que conteste se ensena tal cual. Para poder afirmarlo, la prueba abre `AdministrarParametros.java` y **une las concatenaciones que Spotless parte** —sin eso, un `grep` del mensaje completo sale vacio aunque el texto este ahi letra por letra— y comprueba que el `409 CONFLICTO` que llega a pantalla es el mismo: «El conjunto 2 ya esta sellado; corregirlo exige una version nueva (ADR-0007)» y «El conjunto 3 no tiene ningun parametro: sellarlo vacio diria que el ejercicio esta parametrizado cuando no lo esta». Se arreglan de maneras **opuestas** —una abriendo una version, la otra agregando parametros—, y por eso no se juntan ni se reescriben. <br><br>**Lo que hubo que anadir a la capa de datos de #13, con su motivo.** `pedirCalculo` manda un `POST` **sin cuerpo**, y las tres escrituras llevan la observacion dentro: con el, el borde contestaria el 422 de `Observacion` —«al menos 5 caracteres»— **siempre**, tambien cuando esta bien escrita, o sea que la pantalla no podria guardar nunca y el rechazo hablaria de un dato que si esta. Se anade `enviar()`, sin `senal`: un `GET` abandonado no deja nada detras y por eso `useRecurso` los cancela, pero un `POST` abandonado puede haber llegado ya y cancelarlo en el cliente no lo deshace. Y `useSnapshot`, que no pasa por `useUno` a proposito: el snapshot se cachea un ano con `immutable`, asi que su `sha256` se comprueba **antes** de entregarlo o una copia corrupta no se vuelve a pedir nunca. <br><br>**Y una decision de alcance que se dice para que no parezca un descuido**: `SIN_ARCHIVO` y `DECISIONES` **no van a `datos/prototipo.ts`** aunque su javadoc los anticipe. Por dos motivos y el segundo decide: no los sirve ninguna ruta —son prosa del modulo—, y una seccion no puede importar `prototipo.ts` porque `arranque.ts` lo carga con un `import()` dinamico detras de una bandera para que un `yarn build` de produccion no lleve dentro ni una cifra del corpus. Medido sobre el bundle nuevo: **cero** apariciones de `4600.00`, `4950.00`, `5150.00`, `5350.00`, `5500.00`, `894.27`, `543.16`, `104740.00`, `0.68`, `5e919b37…` y `239a75a0…`. Lo unico del corpus que si viaja son **cuatro** apariciones de `TRAMO_PREDIAL`, y las cuatro son el NOMBRE de la llave —el desplegable de «Agregar parametro» y dos ayudas—, nunca su valor: nombrar la llave es lo contrario de escribir la cifra, es lo que permite pedirla. <br><br>**Lo que este issue NO trae**: los tres cuadros de valuacion y la publicacion del snapshot (#15), que siguen con el hueco del lienzo y su campo «Observación». Y una linea de `Marco.test.tsx` cambio, con su motivo escrito al lado: la del AC7 de #12 decia `getByLabelText('Observación').not.toHaveValue(...)` porque el Panel era el hueco y tenia un campo con el que comparar; con el Panel de verdad puesto ya no hay ninguno, asi que afirma lo que su propio comentario decia desde el principio y es mas fuerte — `queryByLabelText(...)` es **null**. |
| **#7 — el ejercicio 2026 se declaraba SELLADO y el punto 3 de su propia lista no tenía respuesta** (`publicacion/README.md` gana «Los cinco puntos, contestados uno a uno» —una respuesta por punto— y el detalle del **punto 3** con su decisión, su consecuencia medida y por qué este repositorio no la puede comprobar; `ElEjercicio2026SeSellaTest` gana **tres** pruebas —`elPasoCuatroNoSePuedeComprobarDesdeAqui`, que le pregunta a `information_schema` y afirma **la frontera** en vez de cruzarla, y las dos que leen la lista de este README—, más su javadoc y su `@DisplayName`; `publicacion/README.md` entra como **entrada declarada** de `:kamayuk-normativa-parametros:test`; y `CLAUDE.md` deja de decir «SELLADO» sin decir **de qué** — **649 → 652 pruebas**, 0 fallos) | **Nueve roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las nueve—: **(R1)** que la tabla del paso 4 sea una que este esquema **sí** tiene (`depreciacion`); **(R2)** que la consulta del catálogo mire un esquema que no existe; **(R3)** borrar la respuesta del punto 3, que es el estado exacto anterior a este PR; **(R4)** al revés, añadir un punto **6** a la lista sin respuesta; **(R5)** que el encabezado de las respuestas cambie de nombre; **(R5b)** que cambien **los dos** encabezados, que es el caso del sujeto vacío; **(R5c)** con R5b puesta, **quitarle a la guarda sus dos centinelas**; **(R6)** que la respuesta del punto 3 pierda la llave con la que `catastro` se para; **(R7)** que pierda el guion del paso 4; **(R8)** quitar la entrada de Gradle del README y repetir R3; y **(R9)** renombrar el encabezado del punto 3 | **1, 1, 1, 1, 1, 1, —, 1, 1, VERDE y 1 en rojo.** <br><br>**(R3) es el issue reproducido en un mensaje**, y no hubo que inventarlo: es el estado del que salió este PR. «Expecting actual: `[1, 2, 4, 5]` to contain exactly (and in same order): `[1, 2, 3, 4, 5]` but could not find the following elements: **`[3]`**», con el `as` diciendo «cada punto de «Antes de sellar» tiene su respuesta, y en su orden. El que falte se queda sin contestar sin que nada lo diga, que es lo que paso con el 3 hasta #7». **(R4)** es su mitad simétrica —una lista que crece sin que crezcan las respuestas— y da «to contain exactly … `[1, 2, 3, 4, 5, 6]` … could not find … `[6]`»: hacen falta las dos, porque un punto sin contestar y una respuesta de más se arreglan en sitios distintos. <br><br>**(R5c) es el hallazgo, y pasó en VERDE.** `containsExactlyElementsOf` **no puede ver** el único caso que de verdad importa: si las **dos** listas se quedan vacías, la afirmación es cierta sobre el conjunto vacío. Medido: con R5b puesta —los dos encabezados renombrados— y los dos centinelas quitados, la guarda sale **PASSED** y `BUILD SUCCESSFUL`, informando en verde de que la lista está contestada sobre **cero puntos**. Con los centinelas, la misma rotura da «**no se leyo ni un punto de la lista «Antes de sellar» de README.md**: o la seccion cambio de nombre o la tabla se fue, y esta comprobacion no midio nada». Es C-15/C-16 por el eje de un archivo de documentación. <br><br>**(R8) es la lección de #192 §2, y también pasó en verde.** Sin declarar `publicacion/README.md` como entrada de `test` —que es el estado de `main`—, borrarle la respuesta al punto 3 deja **`> Task :kamayuk-normativa-parametros:test UP-TO-DATE` · `BUILD SUCCESSFUL in 6s` · `23 actionable tasks: 23 up-to-date`**: la guarda no se rompe, **deja de correr**. Con la entrada puesta, la misma rotura ejecuta la tarea («1 executed») y da el rojo de R3. El módulo ya declaraba así sus otros tres archivos del corpus; éste faltaba porque nadie lo leía todavía. <br><br>**(R1) y (R2) son las dos mitades de la afirmación de frontera, y se rompen en direcciones opuestas.** (R1) —la tabla del paso 4 apuntando a una que sí está— da «`depreciacion` es de `catastro` y la carga el paso 4 (`cargar-arancel-vial.sh --conjunto-id N`), entre abrir y sellar. No esta en este esquema, asi que esta clase sella sobre una precondicion que NO PUEDE VER», con las 21 tablas del esquema listadas debajo. (R2) —la consulta apuntando a un esquema inexistente— da el contraste que impide que «arancel no está» sea cierto sobre la nada: «si esta consulta no encuentra las tablas de este sistema, no esta mirando este esquema y «arancel no esta» seria cierto sobre la nada: **no se habria medido nada**», con `Expecting LinkedHashSet: [] to contain: ["conjunto_parametros", "parametro_tributario", "depreciacion"]». <br><br>**(R6) y (R7) separan «no está cargado» de «no está cargado y esto cuesta».** (R6), sin la llave: «y la consecuencia MEDIDA, que es la llave con la que `catastro` se para (`ValorizacionDelPredio`). Sin ella la respuesta es «faltan cosas», que es lo que este repositorio lleva prohibiendo desde que la lista nombra sus diez filas una a una». (R7), sin el guion: «la respuesta tiene que nombrar el paso que no se corrio; sin el, «no esta cargado» no dice que habria que correr». Y (R9) es el centinela de esa lectura: «no se encontro la seccion que contesta al punto 3 en README.md: esta comprobacion no midio nada». <br><br>**La afirmación se hace sobre `information_schema` y NO es un cruce de frontera, que es lo que la hace escribible.** `arancel` es tabla de `catastro` en el reparto de `ConfiguracionDeNormativa`, así que la undécima regla prohíbe consultarla desde aquí; preguntarle al **catálogo del motor** si existe es lo contrario de consultarla, y es la única forma que este repositorio tiene de afirmar dónde acaba. El escáner de la regla 11 recorre además sólo `/src/main/` —medido en `FronteraDeSistemaTestBase.esFuenteDeProduccion`—, y esto es `src/test`. <br><br>**La consecuencia del punto 3 no se estimó: estaba medida del otro lado, y hay que leerla.** `ValorizacionDelPredio` comprueba el arancel como **cuarta precondición** —después de los dos cuadros y **antes** del `% actualización`— y sin él devuelve `SinValorizar` con la llave `ARANCEL:‹ejercicio›`. Y el censo del padrón de `catastro` lo escribe como **premisa suya**: «el arancel de cada vía **se supone publicado** … el padrón de demostración NO trae ninguno … **sin la premisa, los 23 predios saldrían por `ARANCEL:2026`**». O sea que el **«4 de 23»** que este `CLAUDE.md` cita —y que la firma de §1.6 desbloqueó— **se mide suponiendo cargado el arancel**; en una instalación que no haya corrido el paso 4 son **0 de 23, los 23 por `ARANCEL:2026`**, antes del `% actualización` y antes de RT-004. La firma sigue valiendo lo que se midió; lo que no vale es leer «4 de 23» como una promesa sobre una municipalidad sin arancel. <br><br>**Y «sellado» se midió también, porque la palabra decía más de lo que hay.** No consta en este repositorio ninguna corrida de `--sellar` contra un ambiente: lo último anotado contra `stg` es del 2026-08-29 y dice, con esas palabras, «lo que **no** se hizo, a propósito: `--sellar`». Lo que hay sellado es el conjunto que `ElEjercicio2026SeSellaTest` **compone contra PostgreSQL real**, con lo que este repositorio publica. `CLAUDE.md` lo dice ahora así, y con ello dice **de qué** está sellado: 33 filas y dos cuadros —lo de norma nacional— y **qué precondición no se comprobó**. <br><br>**Y el estado de `infrastructure#11` se midió el 2026-09-07 en vez de repetirse, y el enunciado había quedado atrás.** Ese issue **se cerró ese mismo día** (PR #34, `fd30d7b`) y con él los **tres** guiones de `infra/carga-de-datos/` ya resuelven deployment, base y namespace por sistema y ambiente. Aun así el paso 4 sigue sin poderse correr, por dos cosas que su propio cierre declara: `cargar-arancel-vial.sh` **no es ninguno de esos tres** —vive en `hneyra/catastro` y quedó en `GUIONES_PENDIENTES` con `hneyra/infrastructure#10`, junto a otros trece—, y la credencial de `rol_carga_parametros` **sigue sólo en el espacio de nombres de la plataforma** (su AC-2, declarado **no cumplido**), con una guarda allí que lo mide y se pondrá roja el día que aterrice. <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16.13 en el puerto 5435, `./gradlew build --rerun-tasks --continue`, sumando los XML de `test` y `pruebaDeArranque` de los siete módulos): **649 → 652**, 0 fallos. Las **+3** son exactamente las tres pruebas nuevas, y el módulo `parametros` pasa de 112 a 115. Los nueve verificadores del corpus y de las muestras, **verdes los nueve** antes y después. `verificarArquitectura` y `verificarAislamiento` **no los invoca `build`** —medido: ni una aparición suya en su registro—, así que se corrieron aparte y salen **`BUILD SUCCESSFUL`**. **Y la línea base hubo que medirla DOS veces**: la primera corrida salió con 2 rojos y un `compileTestJava FAILED`, y ninguna de las tres cosas era de `main` — los dos rojos eran las pruebas de contrato con `catastro` y `rentas`, que **no se saltan** cuando falta el clon hermano («una prueba de contrato que no encuentra su contrato y pasa en verde es peor que ninguna») y en ese directorio no estaban; y el fallo de compilación era **mío**, por editar el árbol mientras la medición corría. Se repitió con los cuatro clones hermanos al lado y en un *worktree* aparte e intacto: **BUILD SUCCESSFUL en 12 m 17 s, 649 y 0 fallos**. <br><br>**Lo que NO cierra, con su motivo.** (a) **El punto 3 sigue sin contestarse corriéndolo, y eso no depende de este repositorio**: hacen falta el guion del paso 4 (`hneyra/infrastructure#10`) y la credencial en el namespace del Job (el AC-2 de `hneyra/infrastructure#11`, cuya mitad pendiente es una entrada de `claves()` en **el descriptor de este repositorio** y es otro issue). Este PR **no lo espera**: lo que pedía el AC-5 era resolverlo o declararlo, y se declara. (b) **No se desella nada ni se revierte `#5`**, que es lo que el propio issue pide no hacer. (c) **No se firma nada**: ninguna cabecera del corpus se toca y ningún archivo cambia de estado — lo que faltaba aquí no era una firma sino una respuesta escrita. (d) **AC-3 no lo vigila ninguna máquina**: la guarda nueva lee `publicacion/README.md`, no `CLAUDE.md`; que la fila de «El ejercicio 2026» siga diciendo de qué está sellada lo lee la revisión. (e) **Los tres archivos `.md` del corpus que esta misma clase lee** —`predial-porcentaje-de-actualizacion.md`, `valores-unitarios-2026.md` y `depreciacion.md`, por `estadoDelArchivoDelCorpus`— **siguen sin ser entrada declarada de Gradle**, o sea que devolver uno a `TRANSCRITO` deja la tarea `UP-TO-DATE` igual que hacía el README hasta este PR. Es el mismo defecto por el mismo eje, se declara medido y no se arregla aquí: cerrarlo bien es declarar el directorio del corpus, y eso cambia qué vuelve a correr en cada PR de documentación |
| **Etapa 4 de [`infrastructure`#52](https://github.com/hneyra/infrastructure/issues/52), el carril de `normativa` ([`identidad`#4](https://github.com/hneyra/identidad/issues/4)) — la copia local de la autorización deja de sembrarse a mano y pasa a LEERSE del buzón de `identidad`** (`V2__consumidor_de_identidad.sql`: `identidad_evento_aplicado` —la deduplicación, con la clave `(municipalidad_id, evento_id)`— e `identidad_evento_muerto` —lo que no se pudo aplicar nunca, con el `cuerpo` ENTERO en `text` y su `motivo`—, las dos con RLS `ENABLE`+`FORCE` y sin `DELETE`; en `kamayuk-normativa-seguridad`, **13 clases nuevas de `src/main`** —`BuzonDeIdentidad`/`CopiaLocalDeLaAutorizacion`/`EventoRecibido`/`TipoDeEventoDeIdentidad` en `dominio.consumidor`; `ClienteHttpDelBuzonDeIdentidad`, `TokenDeServicioDeKeycloak` (copiado de `rentas`), `CopiaLocalDeLaAutorizacionJdbc` con los **siete tipos** por clave natural, `AlertaAlCanalDelResponsable` y `ConfiguracionDelConsumidorDeIdentidad` en `infraestructura.consumidor`; y en `aplicacion` `AplicarUnEventoDeIdentidad` (`REQUIRES_NEW` por hecho), `ConsumirEventosDeIdentidad` (la vuelta: aplica, aparta o posterga, y acusa DESPUÉS) y `CorrerElConsumidorDeIdentidad` (el `ApplicationRunner` del perfil `batch`, cincuenta vueltas y parada por «sin progreso»)—; `ImplantarMunicipalidad` termina con una pasada del consumidor; el descriptor gana el `CronJob` `*/5 * * * *` con `concurrencyPolicy: Forbid` y `backoffLimit: 1`, la credencial `identidad` con `emisor: "keycloak"` y **la primera arista de egreso hacia un sistema**; `despliegue/compose.yaml` le da a la implantación las mismas seis variables; `docs/50-api/contratos-que-consume/identidad.json`, el contrato que `identidad` comprobará en SU CI, con su entrada de Gradle; y `escritoresDeLaAutorizacionConMotivo()` deja de ser `null`: la regla 12 pasa de «no se ha mirado» a **«SÍ se vigila aquí»** — **660 → 694 pruebas** en el backend, **13 → 17** en el descriptor) | **Seis roturas, cada una aplicada SOLA sobre `src/main` —o sobre el descriptor, o sobre la configuración de las barreras— y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las seis—: **(R1)** acusar ANTES del `COMMIT`, o sea `consumir()` `@Transactional` con `aplicar` en `REQUIRED`: una transacción por vuelta y el acuse dentro; **(R2)** `REQUIRES_NEW` → `REQUIRED` en `aplicar`, o sea dos municipalidades en la misma transacción; **(R3)** tratar `DependenciaQueNoLlego` como `NoSePuedeAplicar`: apartar y acusar un fallo transitorio; **(R4)** quitar la comprobación de `sistema` en `escribirPermiso`, o sea aplicar como propio un `PERMISO_FIJADO` de otro sistema sobre una opción homónima; **(R5)** `suspend: true` en el `CronJob`; y **(R12)** quitar `CopiaLocalDeLaAutorizacionJdbc` de `escritoresDeLaAutorizacionConMotivo()` | **6, 1, 2, 1, 1 y 1 en rojo.**<br><br>**(R1) es la propiedad que el AC-2 existe para sostener, y su rojo lleva los tres identificadores dentro**: «[AC-7 (1)] el hecho cuyo commit fallo NO puede estar acusado: acusado, `identidad` no lo vuelve a servir y se pierde para siempre. Y lo que iba delante, confirmado, SI] Expecting actual: [0b7e78a3…, b6ea9248…, 490f07f1…] to contain exactly (and in same order): [0b7e78a3…]» — con el acuse dentro de la transacción, el `COMMIT` que un `CONSTRAINT TRIGGER` diferido hace fallar llega **después** de que el acuse ya viajó, y el alta se pierde con el buzón diciendo «entregado». Los otros cinco son la misma rotura vista desde otros sitios: AC-7 (2) cae también —con `REQUIRED` el hecho de B se escribe bajo el `SET LOCAL` de A—, y tres salen con «`UnexpectedRollbackException: Transaction rolled back because it has been marked as rollback-only`», que es lo que pasa cuando un `apartar` `REQUIRES_NEW` vive dentro de una vuelta que ya se marcó para deshacer.<br><br>**(R2) da exactamente 1 y es el AC-7 (2) con su mecanismo escrito**: «[AC-7 (2)] con REQUIRED el hecho de B se escribiria bajo el SET LOCAL de A —las escrituras ponen current_setting(), no un parametro— y la cuenta de B apareceria en A, sin un solo error] Expected size: 1 but was: 0». La prueba abre una transacción con la municipalidad A por fuera, cambia el `TenantContext` a B y consume: con `REQUIRES_NEW` la fila de B aterriza en B; sin él, en A. **Y es el motivo por el que las escrituras de la copia no reciben `municipalidadId`** (regla 2): lo ponen con `current_setting('app.municipalidad_id')`, así que quien abre la transacción decide en qué inquilino cae, y por eso cada hecho tiene que abrir la suya.<br><br>**(R3) da 2**, y el primero dice el daño entero: «[AC-7 (3)] la afiliacion cuyo grupo no llego se POSTERGA —la vuelta la nombra y no la acusa— y no se aparta: apartada, `identidad` ya no la sirve y el grupo que llegue despues se queda sin su miembro para siempre, sin un solo error] expected: 4e156462… but was: null». El segundo es el runner: «[un hecho postergado no se acusa y volveria en cada vuelta: se para a la primera] expected: 1 but was: 2» — con el postergado apartado y acusado, la vuelta siguiente ya no lo ve y el runner da una vuelta de más creyendo que avanzó. **Y la primera versión de ese rojo era muda** —«expected: 4e156462… but was: null», sin una palabra de por qué—: se le escribió su `.as(...)`, que es la lección que `identidad` dejó en su etapa 3.<br><br>**(R4) da 1**: «[un permiso de otro sistema no es de esta copia] expected: 1 but was: 0» — `permisos` es una opción de `rentas` **y** de `identidad`, y `parametros` podría ser mañana de cualquiera; sin la comprobación, un permiso concedido sobre la opción de otro sistema con el mismo código **aparecería aquí como concedido**, y la única cifra que lo delata es la de la prueba. Se ignora **con aviso y acusado**: no es un hecho roto, es un hecho que no habla de esta copia.<br><br>**(R5) da 1 en `descriptor.test.ts`**: «el consumidor nacio suspendido (AC-7 de identidad#4): expected true not to be true». Y se escribe así —`not.toBe(true)` y no `toBe(false)`— porque `undefined` es lo que Kubernetes lee como «no suspendido» y declarar `suspend: false` sería ruido en el manifiesto (la forma de `rentas` #21 AC-4).<br><br>**(R12) es la regla 12 mordiendo por primera vez en este repositorio, y nombra la clase con sus CINCO líneas**: «`kamayuk-normativa-seguridad/src/main/java/kamayuk/normativa/seguridad/infraestructura/consumidor/CopiaLocalDeLaAutorizacionJdbc.java` — ADR-0039: la autorizacion es un sistema y su dueño es «identidad». Esta clase ESCRIBE usuario/grupo/miembro/permiso … Se pide por el API de «identidad», o se declara en escritoresDeLaAutorizacionConMotivo() con su motivo y su fecha de fin: linea 139: INSERT INTO usuario, linea 161: INSERT INTO grupo, linea 191: INSERT INTO miembro, linea 249: INSERT INTO permiso, linea 264: INSERT INTO permiso». Con la lista entera, `verificarArquitectura` imprime «[ADR-0039] la escritura de usuario/grupo/miembro/permiso SI se vigila aqui; escritores declarados: [CopiaLocalDeLaAutorizacionJdbc, SembradorDeLaCopiaLocal]» —medido en el `system-out` de `ProhibicionesEnElCodigoFuenteTest`— y el build sigue verde. **`SembradorDeLaCopiaLocal` se queda declarado hasta la etapa 5**, que es cuando la implantación dejará de sembrar desde el catálogo.<br><br>**Lo que ADR-0025 afirma y lo que esta etapa cambia, dicho con la cita que AC-3.3 pide.** ADR-0025 dice que `normativa` «no llama a ningún otro sistema», y este repositorio lo repetía en tres sitios como afirmación sobre la arquitectura: la cabecera del descriptor, su README y este `CLAUDE.md`. Los tres cambian y **ninguno se contradice**: para sellar, `normativa` sigue sin consultar a nadie —la pregunta que dejaban escrita, «qué dato de otro sistema hace falta para sellar una cifra que la ley ya fijó», sigue contestándose «ninguno»—. Lo que lee del buzón de `identidad` no es un dato de negocio: es **la réplica de la autorización**, que desde ADR-0039 tiene un dueño que no es este sistema, y [ADR-0039 §«Lo que cuesta»](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0039-la-identidad-es-un-sistema.md) lo dice con esas palabras. La arista la lleva el perfil `batch` y no el proceso web, así que con `identidad` caído la ventanilla sigue leyendo parámetros: lo que se pierde es la frescura de la copia, no la capacidad de autorizar. Y la arista apunta a `componente: identidad-sistema` en `kamayuk-identidad-<ambiente>`: `componente: identidad` en la plataforma es Keycloak, y la prueba del descriptor exige que **las dos** sigan.<br><br>**Cinco decisiones con su motivo, y ninguna es de gusto.** (1) **Un hecho que no se sabe aplicar se APARTA** —tipo desconocido, cuerpo que no es JSON, permiso sobre una opción que el catálogo no tiene—, al revés que `rentas`#54, que ignora con aviso: allí un tipo del territorio es una capacidad que no existe y el hecho está bien; aquí los siete tipos son la autorización entera y un octavo no tiene sujeto —es lo que el `CHECK` de `identidad_evento` impide del otro lado—, así que lo que llega mal está mal, se guarda con su cuerpo entero para poder explicarlo, se acusa y se avisa. (2) **Una dependencia que no llegó NO se aparta ni se acusa: se posterga y la vuelta se corta EN ORDEN** —la afiliación antes que su grupo, el permiso antes que su cuenta—, porque el orden es contenido (`identidad` etapa 3, R4) y saltar el hecho para seguir con el siguiente reordenaría la cola; el `INSERT … SELECT` que escribe 0 filas es lo que lo detecta, y **falla nombrando qué falta**, que es el defecto que `identidad` encontró en su propio instrumento (`AplicadorDeReferencia`, etapa 2). (3) **Un fallo de la base a mitad de vuelta acusa lo ya confirmado y sale entero**: no acusarlo solo haría que se sirviera otra vez y se deduplicara, y tragárselo dejaría la vuelta en verde con la copia a medias. (4) **Un `PERMISO_FIJADO` de otro sistema se ignora con aviso y se acusa**, no se aparta: el hecho no está roto, habla de una opción que esta copia no guarda. (5) **La implantación no cae si `identidad` no contesta**: la municipalidad ya está, el consumidor lo dice con la causa y el `CronJob` lo trae en su ventana; tumbar el `Job` dejaría un despliegue sin municipalidad por un sistema que no es este, que es lo que ADR-0039 §«Lo que cuesta» (2) excluye.<br><br>**Y una sexta que la revisión del integrador obligó a tomar al revés de como estaba, y es la medida de `catastro`.** `ResponsableDeLaCopiaLocal` nació copiado de `ResponsableDeLaProyeccion` de `rentas`, que exige un canal `http(s)` para poder comprobar la entrega ejecutándola (C-8); medido, el canal que los dos stacks declaran es un **correo** —`kamayuk:canalDeOperacion: operaciones@example.pe`, y `KAMAYUK_CANAL_DE_OPERACION` en el `.env` de la plataforma—, y con él el `Job` de implantación —que termina con una pasada del consumidor— **no habría arrancado en ningún ambiente**: la regresión de C-7, «el pod no levanta», y no un hallazgo que se pueda dejar declarado. El carril de `catastro` midió exactamente lo mismo antes de copiar la clase y tomó la decisión contraria en su `ResponsableDelConsumidor`, y aquí se adopta **la misma, con el mismo motivo**: el canal es obligatorio y NO tiene que ser `http(s)`; el aviso se escribe **siempre** en el registro con nivel ERROR y el responsable con su canal dentro —que es sobre lo que alerta INF-11—, y **además** se entrega con un `POST` cuando el canal es una dirección `http(s)`. El contraste es `AvisoAlResponsableTest`: con `operaciones@example.pe` el responsable se construye, el aviso queda como una sola línea de ERROR con «Equipo de operacion <operaciones@example.pe>» y el motivo dentro, y no sale ningún `POST`; con `http://127.0.0.1:<puerto>` la misma línea queda **y** el cuerpo llega con `"responsable"`, `"motivo"` y `"apartadosSinExplicar"`; un canal que no contesta añade su segunda línea y no tumba nada. Lo que cuesta queda dicho: con un correo, la única constancia es esa línea. **Y ese contraste destapó un defecto en el propio instrumento**: la prueba del `POST` salió roja con «`HttpTimeoutException: request timed out`» porque `ServidorDeMentira` leía `Content-Length` **caracteres** con un `BufferedReader`, y un cuerpo con un guion largo tiene más bytes que caracteres —se quedaba esperando lo que nunca llegaba, el cliente agotaba sus diez segundos y la respuesta salía cuando el cliente cerraba—; o sea que las tres entregas de `ConsumidorDeIdentidadJdbcTest` «llegaban» diez segundos después de que el remitente las diera por perdidas, y el verde tapaba un minuto de esperas. Corregido a bytes, el módulo pasa de 1 m 12 s a 40 s.<br><br>**Y una guarda que ya existía enseñó dónde entra el consumidor en las barreras.** `verificarArquitectura` salió rojo a la primera con la regla 10: «el metodo AplicarUnEventoDeIdentidad.aplicar(EventoRecibido, Instant) escribe dentro de una transaccion y no recibe una Observacion». Es correcto que la pida y correcto no dársela: lo que escribe es una COPIA de un dato que este sistema no produjo, ya observado en `identidad` en el acto que lo cambió, y pedir una aquí produciría la cadena fija que el javadoc de la regla advierte. Entran las dos —`aplicar` y `apartar`— en `escriturasSinUsuarioQueObserve()`, **las primeras de este repositorio**, con el mismo motivo que `AplicarUnHecho` tiene en `rentas` y el acuse en `identidad`; la entrada la vigila `SujetosDeLaConfiguracion` (#27) en las dos direcciones.<br><br>**Cifras, medidas en esta máquina** (PostgreSQL 16 nativo en el 5432, JDK 25, `./gradlew build verificarArquitectura verificarAislamiento verificarArranque --continue --rerun-tasks --max-workers=2 --no-daemon` con los `-Dkamayuk.pruebas.postgres.*`, sumando los XML de `test` y `pruebaDeArranque` de los siete módulos): la línea base sobre un árbol **extraído de `origin/main` (`fd8aead`) con `git archive`** —no el árbol de trabajo— da **660 pruebas, 0 fallos, 0 omitidas**, `BUILD SUCCESSFUL in 3m 45s`; esta rama da **694, 0 fallos, 0 omitidas**, `BUILD SUCCESSFUL in 3m 10s`. Las **+34** son exactamente las de este trabajo, contadas módulo a módulo: `seguridad` **7 → 39** —15 casos de `ConsumidorDeIdentidadJdbcTest`, con sus tres clases anidadas, 12 de `ElTokenDeServicioDeIdentidadTest` y 5 de `AvisoAlResponsableTest`— y `aplicacion` **108 → 110**, las dos de `ContratoQueConsumeDeIdentidad`; los otros cinco módulos no se mueven (154 · 43 · 115 · 181 · 48, y las 4 de `pruebaDeArranque`). `cd infrastructure && yarn verificar` **13 → 17**. **Y la primera corrida completa de la rama salió roja en `checkstyleMain` con las pruebas en verde** —un import sin usar y un `switch` sin `default`—, que es el rojo que `:test` no ve y CI sí (la lección de `rentas`#54 con Spotless): se arregló y se volvió a correr entera. La línea base tuvo que medirse **dos veces**: la primera murió con 3 rojos que no eran de nadie —`ContratoConRentasTest`, `ContratoConCatastroTest` y `CatalogoDelSistemaTest` buscan la raíz del clon por `.git`, y un árbol de `git archive` no lo tiene—; se le dio el marcador y la segunda es la que vale, y terminó a las 15:18 UTC, **antes** del reinicio de la máquina de las 18:17 que se llevó todo lo que corría en segundo plano.<br><br>**Lo que NO se cierra, con su motivo medido.** (a) **`identidad` no se toca**: el día que esto aterrice, su `ContratosDeLosConsumidoresTest.ningunoPublicaTodaviaSuContratoConIdentidad` se pondrá rojo diciendo que `normativa` ya publica, y hay que quitarle el `@Disabled` a su `ContratoConNormativaTest`; es su carril. (b) **`infrastructure` no se toca, y tres de sus censos se mueven con esto**, medido corriendo sus guardas contra este árbol: `espera-al-motor` pasa de 12 a **14** procesos que hablan con el motor —este `CronJob` y otro de esta misma etapa—, `TECHO_DE_LOS_SISTEMAS` de C-14 pasa de 1200m a **1300m** en el pico de los dos ambientes (este `CronJob` pide 50m/256Mi, como el ingestor de `rentas`), y la guarda `identidad-de-servicio` exige `{"sistema":"normativa","llamaA":"identidad"}` en `200101.json` y `200105.json` —que su carril ya declara: los cuatro rojos que quedan allí nombran a `rentas` y a `caja`, no a este sistema—. `compose-de-los-sistemas` y `variables-sin-omision` salen verdes con este compose y este `application.yaml`. (c) **El defecto latente de `ResponsableDeLaProyeccion` de `rentas` queda dicho como ajeno y sin arreglar**: exige `http(s)` y el canal de los stacks es un correo, así que su `CronJob` del ingestor no arranca en el clúster con esa configuración; aquí la misma clase se corrigió antes de que costara (la sexta decisión de arriba), y allí es de su dueño. (d) **La ventana de inconsistencia no está medida**: el `CronJob` declara cinco minutos como cota superior, y lo que ADR-0039 §«Lo que cuesta» (2) exige medir es de la etapa 5. (e) **La huella no se verifica**: `identidad` la calcula sobre el cuerpo y aquí viaja hasta `identidad_evento_aplicado.huella` sin recomputarse; comprobarla exige el mismo algoritmo de los dos lados, que hoy vive solo allí. (f) **No hay «descartado por viejo»**: las cuatro tablas de la copia no llevan secuencia, así que un hecho que llegara después de otro más nuevo del mismo sujeto lo pisaría; hoy no puede pasar porque `identidad` sirve en orden de `id` y la vuelta se corta en orden, y se dice para que nadie lo suponga al revés. (g) **`SembradorDeLaCopiaLocal` sigue sembrando desde el catálogo** en cada implantación —es la etapa 5—, así que entre esta etapa y aquella la copia nace del catálogo **y** se corrige desde el buzón. (h) **Nada de esto se levantó contra las dos aplicaciones ni contra un Keycloak**: aquí no hay demonio de Docker. Lo medido va de HTTP a PostgreSQL con un buzón de mentira que sirve, acusa y rechaza como el de `identidad` (`BuzonDeMentira`), y con un emisor de mentira sobre `ServerSocket` que registra la petición que LLEGÓ. **Y dos cosas del entorno que hay que dejar dichas, porque las dos pasaron**: otro agente enlazó `scratchpad/e4-base/normativa` como símbolo hacia **este** árbol de trabajo, y a las 18:26 UTC su copia dejó un `normativa/normativa/backend/…` dentro de él —29 archivos, ninguno versionado—, que se borró dos veces; y el borrador de ESTA fila, escrito en un archivo del directorio compartido, **lo sobrescribió el carril de `caja` con el suyo** y llegó a estar pegado aquí como fila 273 durante unos minutos. Se detectó porque la fila pegada no tenía los marcadores que la mía sí llevaba; se quitó, se reescribió en un directorio privado y de ahí sale esta. Una línea base, y un borrador, se guardan donde nadie más escribe. |
| **La corrección de [`normativa`#30](https://github.com/hneyra/normativa/pull/30) que salió de MEDIR AC-5 y AC-6 con las cinco aplicaciones levantadas — un hecho pospuesto dejaba de congelar la copia entera, y al responsable le llega el aviso que no le llegaba** (`ConsumirEventosDeIdentidad` deja de cortar la vuelta: aplica lo que se puede aplicar y retiene **solo lo que escribe la misma fila** que algo ya pospuesto, con `CopiaLocalDeLaAutorizacion.filaQueEscribe` como clave natural; `correr()` —la corrida entera, que hasta ahora vivía en el runner— avisa **una vez** de los pospuestos que pasan de `EDAD_QUE_SE_AVISA` = **15 minutos**, tres ticks del `CronJob`, medidos contra el `creadoEn` que publica `identidad`; `AlertaDeEventosSinAplicar` gana `hayEventosPospuestosDesdeHaceRato` y `AlertaAlCanalDelResponsable` la implementa con la lista —tipo, sujeto, secuencia, edad—; los hechos de otro sistema pasan de **un WARN cada uno a una línea de resumen por vuelta**; y `BuzonDeIdentidad.acusar` devuelve **cuántos quedan DESPUÉS del acuse** — **694 → 702 pruebas**) | **Cinco roturas, cada una aplicada SOLA sobre `src/main` y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las cinco—: **(N1)** quitar la retención por FILA, o sea aplicar todo lo que va detrás de un pospuesto; **(N2)** devolver el `break`, que es el estado exacto de `909724f`; **(N3)** que el aviso no mire la edad y avise de cualquier pospuesto; **(N4)** devolver el WARN por cada hecho ajeno; **(N5)** que «quedan» vuelva a salir del lote, o sea contado antes del acuse | **1, 3, 1, 2 y 1 en rojo — y el hallazgo es que N5 pasó primero en VERDE por un defecto del instrumento.**<br><br>**Lo que se midió antes de decidir, porque el encargo dejaba las dos salidas abiertas.** El ensayo con las cinco aplicaciones (`ac56/informe.md` §3(d)) midió que cortar la vuelta en el primer pospuesto deja esta copia **sin recibir ningún cambio** hasta que ese hecho se resuelva: con una afiliación pospuesta —un grupo que esta copia había perdido—, el alta de un usuario que iba detrás y **no dependía de nada** no entró, mientras `rentas` y `caja` sí la aplicaron, y a nadie le llegó un aviso (H7: cero avisos en dos corridas). Una fila perdida congelaba la autorización entera, en silencio y sin fecha. **Y aplicar todo lo de detrás tiene su propio caso, también medido**: una afiliación pospuesta, el `GRUPO_MODIFICADO` que la desbloquea llegando detrás, y la **desafiliación de ese mismo par** aplicándose antes que su afiliación — la vuelta siguiente aplica la afiliación pospuesta encima y la copia dice que esa cuenta está en un grupo del que `identidad` la sacó: **no desordenada, falsa**. Así que no se elige entre los dos daños: lo que se retiene no es «todo lo de detrás» sino **los hechos que escriben la misma fila** que uno ya pospuesto. Es una desviación de lo que hoy hacen `rentas` y `caja`, y se dice con su motivo.<br><br>**(N1) da 1 en rojo, y es ese caso**: «[la desafiliacion escribe LA MISMA FILA que la afiliacion pospuesta, asi que espera con ella. Aplicada aqui, la vuelta siguiente le pondria encima la afiliacion y la copia diria que rquispe2 esta en un grupo del que `identidad` lo saco: no desordenada, FALSA] Expecting actual: [d9d6b48a…] to contain exactly (and in same order): [d9d6b48a…, a50f3e62…]». Y la prueba **cuenta la fila al final**: tras la segunda vuelta, `miembro.activo` es `false` —afiliado y después desafiliado, en ese orden—, que es lo que la copia tiene que decir.<br><br>**(N2) da 3, y el primero es el defecto que el ensayo midió**: «[el ensayo de AC-5/AC-6 midio lo contrario: cortar la vuelta aqui dejaba esta alta —que no depende de nada— sin entrar, y con ella cualquier cambio posterior, hasta que el grupo perdido volviera. Una fila perdida congelaba la autorizacion entera] Expected size: 1 but was: 0». El tercero enseña algo que no se buscaba: con el corte, el aviso de los pospuestos viejos **lista uno de los dos**, porque al segundo no se llega nunca —«Expected size: 2 but was: 1»—, o sea que cortar además deja al responsable con una foto incompleta de lo que está parado.<br><br>**(N3) da 1 y cae en el CONTRASTE**, que es donde tiene que caer: «y uno de dos minutos NO avisa: todavia es una carrera, no una averia … Expecting empty but was: [EventoRecibido[… creadoEn=2026-09-09T11:58:00Z]]». Los quince minutos son **tres ticks** y no uno: el primero es normal —un hecho puede llegar en la misma página que su dependencia—, el segundo todavía puede serlo si una corrida se solapó o falló con `backoffLimit: 1`, y a los quince ya no es una carrera. La edad se mide contra el `creadoEn` del emisor y no contra cuándo lo leímos: lo segundo se reinicia con cada corrida y no diría nunca que algo lleva quince minutos. Los otros dos contrastes —una corrida sin pospuestos no avisa, y el aviso es **uno por corrida** y no uno por hecho— van con él.<br><br>**(N4) da 2**: el resumen —«[H6: una implantacion entera producia 161 lineas de WARN, una por permiso ajeno. Lo que se lee de un registro asi es nada] Expected size: 1 but was: 0»— y, de rebote, la prueba de AC-7 (4), que buscaba el texto «OTRO sistema» en la línea por hecho. Son 161 líneas en una implantación entera de este sistema, la peor de los cuatro (H6).<br><br>**(N5) es el hallazgo, y pasó en VERDE la primera vez.** La rotura —volver a contar «quedan» antes del acuse— dejó las 28 pruebas en verde, y el motivo no estaba en el código medido sino en el instrumento: `BuzonDeMentira` servía `quedan` **restando la página**, mientras el emisor de verdad publica la cola ENTERA —«cuantos le faltan en total, contando los de esta pagina. Es su retraso», `EventosController.LoteDeEventosResource` de `identidad`—. O sea que el doble mentía justo sobre el campo que H6 mide, y con él «antes del acuse» y «después del acuse» eran indistinguibles. Corregido el doble a lo que el emisor publica —leído de su fuente, no supuesto—, la misma rotura da 1 en rojo: «[H6 del ensayo de AC-5/AC-6: el «quedan» del lote se cuenta al SERVIR la pagina, o sea antes del acuse, y por eso una vuelta que acusaba 174 decia «174 acusados; quedan 174»] expected: 0L but was: 3L». Ahora el número sale de la respuesta del **acuse**, que es la que el contrato ya declaraba.<br><br>**Lo que este cambio NO cambia, y hay que decirlo**: un pospuesto **sigue sin acusarse** —`identidad` lo vuelve a servir— y **sigue sin apartarse**; la corrida termina en **rc = 0** aunque queden pospuestos, porque un pospuesto no es un fallo sino un hecho esperando a otro, y lo que hace falta cuando la espera se alarga es que alguien se entere, no que un `Job` salga `Failed` cada cinco minutos (que es lo que `catastro` hace hoy, medido: rc=1 en cada corrida). El corte por «sin progreso» tampoco: `acusados == 0` sigue siendo la condición de parada, y con la vuelta siguiendo eso pasa a significar «solo quedan pospuestos», que es exactamente cuando hay que parar.<br><br>**Y una diferencia entre los cuatro consumidores que este carril NO arregla y deja escrita, porque es de los cuatro y no de uno** (H2 del ensayo): **no coinciden en qué es «no se puede aplicar nunca» y qué es «todavía no»**. Ante un `PERMISO_FIJADO` sobre una opción que su catálogo no tiene, este sistema lanza `NoSePuedeAplicar` —lo aparta con su motivo, lo acusa y avisa—, y `rentas` lo deja **pospuesto para siempre**: medido, sus cuatro permisos de las opciones que AC-4 le quitó salen «pendientes por su dependencia» en cada corrida, 4 WARN por vuelta, y cada lote de 200 los trae delante (con 400 pendientes hizo 3 vueltas de 196 donde los otros hicieron 2 de 200). Las dos lecturas son defendibles y salen de dónde se cree que está el defecto: aquí, en el hecho —una opción que este catálogo no tiene no la va a tener por esperar—; allí, en la copia —el acceso podría llegar—. Lo que no es defendible es que cuatro copias de la misma autorización se comporten de tres maneras distintas ante el mismo hecho, y eso es una decisión de ADR-0039 y no de un PR.<br><br>**Cifras, medidas en esta máquina** (PostgreSQL 16 nativo en el 5432 —compartido con otros carriles—, JDK 25, `./gradlew build verificarArquitectura verificarAislamiento verificarArranque --continue --rerun-tasks --max-workers=2 --no-daemon` con los `-Dkamayuk.pruebas.postgres.*`): **694 → 702 pruebas, 0 fallos, 0 omitidas**, `BUILD SUCCESSFUL in 4m 53s`. Las **+8** son 6 en `ConsumidorDeIdentidadJdbcTest` —el orden dentro de una fila, el aviso por edad y sus dos contrastes, «quedan» tras el acuse y el resumen de los ajenos— y 2 en `AvisoAlResponsableTest`; la de AC-7 (3) se reescribe en su sitio y no suma. El descriptor no se toca: **17**. |
| **Etapa 5 de [`infrastructure`#52](https://github.com/hneyra/infrastructure/issues/52), el carril de `normativa` ([`identidad`#5](https://github.com/hneyra/identidad/issues/5)) — el sembrador deja de escribir la autorización, y una implantación que no la consigue FALLA en vez de avisar** (`SembradorDeLaCopiaLocal` → **`SembradorDelCatalogo`** con `git mv`: de SEIS tablas a **dos** —`modulo_sistema` y `acceso`—, fuera los `INSERT` de `grupo`, `usuario`, `miembro` y `permiso`, fuera `GRUPO_DE_ADMINISTRACION` y `otorgarLosSiete`, y `sembrar(cuenta, nombre, porQué)` pasa a `sembrar(porQué)`; `ImplantarMunicipalidad` cambia su tercer paso de un `catch`+WARN a **tres fallos con nombre** (`SinAutorizacion`) y gana la comprobación que ninguno de los tres da: contar las cuentas que quedaron; `CopiaLocalDeLaAutorizacion.cuentasEnLaCopia()` con su implementación y su envoltorio `@Transactional(readOnly = true)`; `escritoresDeLaAutorizacionConMotivo()` pasa de dos entradas a **una**; y el comentario del compose y el del descriptor que prometían lo contrario, corregidos — **702 → 708 pruebas**, descriptor **17 → 17**) | **Seis roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las seis—: **(R1)** devolverle el `INSERT INTO usuario` al sembrador; **(R2)** quitar de la implantación la pasada del consumidor; **(R3)** devolver el tercer paso a su forma EXACTA de la etapa 4 —sin consumidor se dice y sigue; si el buzón no contesta, WARN—; **(R4)** dejar `"SembradorDeLaCopiaLocal"` en `escritoresDeLaAutorizacionConMotivo()`, que es lo que el AC-1 pide medir; **(R5)** quitarle el `@Transactional(readOnly = true)` a `cuentasEnLaCopia()`; y **(R6)** subir la exigencia de la implantación a `ConsumirEventosDeIdentidad.correr()`, o sea generalizarla a toda vuelta | **1, 5, 3, 1, 4 y 5 en rojo.**<br><br>**(R4) es el rojo que el AC-1 pide, y sale de una guarda que ya existía**: «[una entrada que no nombra nada del arbol no exime a nadie hoy, y el dia que nazca una clase con ese nombre la eximira sin que nadie lo haya decidido…] Expecting empty but was: [escritoresDeLaAutorizacionConMotivo() → «SembradorDeLaCopiaLocal»: no hay ninguna clase de produccion con ese nombre, asi que no exime a nadie de ADR-0039. Y esta lista es el trabajo pendiente de la etapa 4 con su fecha de fin…]» —`ArquitecturaTest > ninguna entrada de la configuracion nombra algo que no esta`, 1 de 64—. O sea que **la fecha de fin que la etapa 4 escribió se cobra sola**: no hace falta acordarse de retirar la entrada, dejarla puesta pone el build en rojo. Con la lista ya corregida, `verificarArquitectura` imprime «[ADR-0039] la escritura de usuario/grupo/miembro/permiso SI se vigila aqui; escritores declarados: **[CopiaLocalDeLaAutorizacionJdbc]**».<br><br>**(R1) es la regla 12 mordiendo sobre el sembrador, con archivo y línea**: «`kamayuk-normativa-seguridad/src/main/java/kamayuk/normativa/seguridad/aplicacion/SembradorDelCatalogo.java` — ADR-0039: la autorizacion es un sistema y su dueño es «identidad». Esta clase ESCRIBE usuario/grupo/miembro/permiso, y dos sistemas que escriben la misma tabla de permisos en dos bases no dan un error: dan dos respuestas a «quien puede hacer esto» … **linea 109: INSERT INTO usuario**», 1 de 64. Es la mitad que R4 no cubre: R4 dice que la exención ya no exime a nadie; R1 dice que sin ella la clase no puede volver a escribir.<br><br>**(R2) da 5 de 6 y el rojo del AC-2 es la lista vacía**: «[AC-2] las cinco cuentas que `identidad` publica: el administrador y las cuatro de servicio. Ninguna la escribio esta implantacion: desde la etapa 5 el sembrador no toca `usuario`] Expecting actual: **[]** to contain exactly in any order: ["administrador", "service-account-kamayuk-rentas-servicio-219902", …(3)]». **Y el sexto se queda VERDE a propósito**: es la vuelta del `CronJob`, que no depende de la pasada de la implantación.<br><br>**(R3) da exactamente los TRES casos del AC-3 y deja el AC-2 en verde**, que es la forma que se quería: con el buzón contestando, la implantación de la etapa 4 hacía lo mismo que la de hoy, así que lo único que separa las dos versiones es qué pasa cuando NO contesta. Los tres son «Expecting actual not to be null» sobre `SinAutorizacion`, cada uno con su motivo: «sin `kamayuk.identidad.url` no hay consumidor…», «un 401 es el caso mas probable en un despliegue de verdad…» y «`identidad` esta en pie, contesta 200 y su cola esta vacia porque ESTA municipalidad todavia no se implanto alli».<br><br>**El hallazgo del AC-3: las DOS formas de fallo que el encargo nombra —«sin token o sin buzón»— NO cubren el orden equivocado.** Si `identidad` está en pie y esta municipalidad todavía no se implantó **allí**, el buzón contesta **200 con la cola vacía**: no falta configuración y no falla la red, así que las dos comprobaciones pasan y la copia se queda con cero usuarios. Eso es literalmente «implantar este sistema antes que `identidad`», y es el caso más probable de los tres en un despliegue nuevo. Lo que lo caza es contar **filas de `usuario`** después de la pasada, y por eso el paso 3 tiene tres fallos y no dos. **Y se cuentan filas y no hechos aplicados**, con su contraste medido: `segundaImplantacion` reimplanta sobre el mismo buzón, aplica **cero** hechos —el emisor no vuelve a servir lo acusado— y tiene sus cinco cuentas; con «al menos un evento aplicado» todo segundo despliegue saldría rojo por hacer lo correcto, y (R2) lo enseña por su otra cara: «expected: 5L but was: 0L».<br><br>**(R6) es la decisión que el AC-3 manda tomar, medida, y su rojo son CINCO pruebas de las que TRES son de la etapa 4.** Subir «no queda nadie que pueda entrar» a `correr()` —que es lo que uno escribiría generalizando— pone en rojo la guarda nueva («una vuelta sobre una copia vacia termina normal: el CronJob no falla cada cinco minutos») **y además** «las vueltas paran cuando una no avanza, y no a las cincuenta», «un pospuesto de mas de quince minutos avisa UNA vez por corrida» y «uno de dos minutos NO avisa», las tres con `java.lang.IllegalStateException: La copia local no tiene ni una cuenta: nadie puede entrar`. O sea: la exigencia de la implantación, puesta en la vuelta periódica, se lleva por delante exactamente las propiedades que la etapa 4 construyó —rc=0, un aviso por corrida, umbral de 15 minutos— y deja el `CronJob` en `Failed` cada cinco minutos. Por eso la exigencia vive **en `ImplantarMunicipalidad` y no en el consumidor**, con el motivo escrito en los dos javadoc: son dos preguntas distintas —«¿avanzó la copia?» y «¿quedó alguien que pueda entrar?»— y la segunda sólo tiene sentido en el momento de implantar.<br><br>**(R5) es la respuesta a «qué haría pasar esto en verde», y no pasó en verde: reventó, que es lo correcto y hubo que comprobarlo.** Sin `@Transactional(readOnly = true)`, `cuentasEnLaCopia()` corre fuera de transacción, la política RLS de `usuario` no tiene su `SET LOCAL` y salen 4 de 6 con `BadSqlGrammarException: bad SQL grammar [SELECT count(*) FROM usuario]` cuya causa es `org.postgresql.util.PSQLException: **ERROR: unrecognized configuration parameter "app.municipalidad_id"**`. Es la lección que `rentas` pagó en la etapa 4 con las cinco lecturas de su copia local, aquí antes de que costara: la comprobación no es un `count` cualquiera, es una lectura bajo RLS y **no devuelve cero, revienta**.<br><br>**Y una premisa del encargo salió FALSA, en la dirección que importa: el javadoc y el código de `ImplantarMunicipalidad` se contradecían, y el que estaba en lo cierto era el CÓDIGO.** El javadoc afirmaba «Si el buzon no contesta, la implantacion **si** falla … la corrida sale distinta de cero para que el despliegue la repita», y el código capturaba `IdentidadNoContesta` y hacía WARN (`:152-161`), con su propio comentario explicando por qué NO tumbar el `Job`. Medido: el comportamiento de `origin/main` era el del `catch`, y el argumento del comentario era correcto **para la etapa 4** —con el sembrador puesto, la copia sembrada seguía siendo un estado legítimo—. O sea que no era una contradicción entre el código y su intención, sino un javadoc que se había adelantado a esta etapa. Aquí gana el javadoc porque **cambió el hecho que lo sostenía**, y lo que se escribe en su sitio no es «falla» a secas sino por qué el mismo caso se leía de dos maneras en dos etapas. El comentario del `catch` —«tumbar el Job aqui dejaria un despliegue sin municipalidad por un sistema que no es este»— también deja de ser cierto y se dice: la municipalidad y su catálogo **quedan escritos** (los dos pasos confirmaron antes), lo que no queda es quién pueda entrar, y una prueba lo afirma.<br><br>**Y el `git mv` se hizo y aun así hay que decir lo que NO compra**: el archivo pierde el 60% de su contenido, así que git lo detecta como renombrado a partir de `-M37%` y **no** con el 50% por omisión — o sea que un `git log --follow` a secas sobre `SembradorDelCatalogo.java` se para en este commit. Se dice porque el motivo de usar `git mv` era justamente conservar la historia, y conservarla exige el umbral.<br><br>**Ninguna migración, y estaba medido antes de decidirlo**: `V1__baseline.sql` crea las cuatro tablas de la autorización y **no siembra ni una fila** en ellas, y `V2__consumidor_de_identidad.sql` no las toca; retirar el sembrador no cambia el esquema, cambia quién escribe. Comprobado con `grep` sobre las dos migraciones: cero `INSERT INTO usuario\|grupo\|miembro\|permiso`.<br><br>**El buzón es de mentira y la corriente de eventos es la de verdad, que es lo que el AC-2 pide.** `identidad` no se levanta aquí —sin demonio de Docker en esta máquina— así que `BuzonDeMentira` lo sirve por HTTP con la forma de `EventosController` y el cliente que lo lee es el de producción. Lo que se le publica es lo que emite la implantación de `identidad`, leído de su clon (`ImplantacionEmiteSusEventosTest`, `ImplantarMunicipalidad`, `HechoDeIdentidad`) y copiado **campo a campo** en `CuerposComoLosPublicaIdentidad`: los siete campos del evento, los cuerpos de los cuatro tipos en su orden canónico, `quedan` contando la cola entera y el cuerpo del acuse con `recibidos`/`escritos`/`quedan`. **Se comprobó campo a campo antes de escribir nada**, y el doble ya estaba bien —la etapa 4 de este repositorio arregló su `quedan`, que es el campo con el que tres de los cuatro consumidores midieron mal—. **Cifras de la implantación de cero, medidas**: con los 15 hechos que ese buzón publica —2 grupos, 5 cuentas, 5 afiliaciones y 6 `PERMISO_FIJADO`— la copia local queda con **5 filas en `usuario`, 5 en `miembro`, 2 en `grupo` y UNA en `permiso`**, la del administrador sobre `parametros` con los siete privilegios (`true\|true\|true\|true\|true\|true\|true`); los otros cinco `PERMISO_FIJADO` hablan de opciones de `identidad`, `rentas`, `catastro` y `caja` y se ignoran, acusados. El catálogo lo sigue sembrando este sistema: **un** `modulo_sistema` (`SEGURIDAD`) y **un** `acceso` (`parametros`).<br><br>**Cifras, medidas en esta máquina** (PostgreSQL **16.13** nativo en el 5439, levantado para esto y con `scram-sha-256` sobre TCP; JDK 25.0.4; `./gradlew build verificarArquitectura verificarAislamiento verificarArranque --continue --rerun-tasks --max-workers=2 --no-daemon` con los `-Dkamayuk.pruebas.postgres.*`, sumando los XML de `test` y `pruebaDeArranque` de los siete módulos): la línea base sobre un árbol **extraído de `origin/main` (`e1cdad5`) con `git archive`** da **702 pruebas, 0 fallos, 0 errores, 0 omitidas**, `BUILD SUCCESSFUL in 3m 6s`; esta rama, en un árbol gemelo y contra el mismo motor y la misma copia privada de `infrastructure`, da **708, 0 fallos, 0 errores, 0 omitidas**, `BUILD SUCCESSFUL in 3m 24s`. Las **+6** son exactamente las de `ImplantacionDeCeroTest` y sólo se mueve `seguridad` (**47 → 53**); los otros seis módulos no cambian (114 · 154 · 43 · 115 · 181 · 48). `cd infrastructure && yarn verificar` **17 de 17**, verde antes y después. **Y la línea base hubo que medirla DOS veces, las dos por el entorno y no por el código**: la primera murió con `EXIT=137` —SIGKILL, con cinco carriles construyendo a la vez— y además traía una roja que era artefacto del banco, `un 28P01 nombra la causa`: el motor se había levantado con `--auth=trust`, así que una clave equivocada **se acepta** y la prueba que exige un 28P01 no tiene con qué producirlo. Se cambió `pg_hba.conf` a `scram-sha-256` sobre TCP —comprobando las dos direcciones, que la clave buena entra y la mala da `password authentication failed`— y la segunda medición es la que vale. Una línea base se mide contra un motor configurado como el que usará quien la lea.<br><br>**Lo que NO cierra, con su motivo.** (a) **`identidad` no se toca, y su etapa 5 tiene su propio carril**: aquí no se sabe si su implantación deja de dar de alta algo, y este consumidor aplica lo que le sirvan. (b) **`infrastructure` no se toca**: este PR no mueve ningún recurso del descriptor —`yarn verificar` sigue en 17— y lo único que cambia allí es un comentario que prometía lo contrario de lo que ahora pasa. (c) **Nada de esto se ejerció contra las cinco aplicaciones levantadas**: no hay demonio de Docker en esta máquina, así que lo medido va de HTTP a PostgreSQL con el buzón de mentira, y lo que NO se demuestra es que `identidad` conteste por HTTP exactamente eso —lo que sí se hizo es copiar su forma campo a campo de su código—. (d) **La ventana de inconsistencia sigue sin medirse** (ADR-0039 §«Lo que cuesta» punto 2): exige las cinco aplicaciones en pie. (e) **`despliegue/` no está en `RUTAS_DE_CODIGO`** de la guarda del registro de este repositorio —sí lo está en `caja` desde su #39—, así que un PR que sólo tocara el compose y cerrara un issue no tendría que escribir fila; no es de esta etapa y se deja dicho. (f) **El aviso al responsable no cambia**: un fallo de la implantación no le llega por el canal de INF-11, sale por el código de salida del `Job`, que es lo que el despliegue mira. |
