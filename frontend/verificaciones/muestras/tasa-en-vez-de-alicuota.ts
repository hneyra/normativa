// Viola: un porcentaje se llama «alicuota», no «tasa» (regla 8).
//
// «Tasa» en este dominio es un TIPO DE TRIBUTO —la de limpieza publica, la de parques—,
// asi que llamar «tasa» a un porcentaje hace ambigua cualquier busqueda y cualquier
// conversacion con el area de rentas. En el catalogo de este sistema conviven las dos
// acepciones a la vez, que es donde la ambiguedad deja de ser teorica.

export const tasaInteres = '0.008';

export function aplicar(tasaDeDescuento: string) {
  return tasaDeDescuento;
}
