// Viola: sin tildes ni enie en identificadores (CLAUDE.md §Idioma).
//
// El texto que ve el funcionario SI lleva tildes —va en las cadenas—; lo que no las lleva
// es el identificador, porque un `grep` sobre `alicuota` tiene que encontrarlas todas, y
// en un repositorio cuyo trabajo es que cada cifra se pueda rastrear hasta su norma, una
// busqueda que devuelve la mitad es peor que ninguna.

export const parámetro = 'uit';

export function añoDelEjercicio() {
  return 2026;
}
